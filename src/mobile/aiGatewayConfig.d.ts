export const DEFAULT_UNIFIED_AI_ENDPOINT: string;
/** Feature flag. DEFAULT FALSE — the unified gateway path is dormant unless enabled. */
export const UNIFIED_AI_CHAT: boolean;
/** Canonical v1 gateway endpoint (overridable via EXPO_PUBLIC_UNIFIED_AI_ENDPOINT). */
export const UNIFIED_AI_ENDPOINT: string;
