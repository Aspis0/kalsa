{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "brain-events",
  "description": "The walk reports where it has got to on the event bus, and the first screen is the only reader. Without this grant the listen call's promise never settles and the whole walk -- measuring the machine, then a download that can be twenty gigabytes -- happens behind a page that says nothing.",
  "windows": ["main"],
  "permissions": ["core:event:default"]
}
