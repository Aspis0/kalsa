{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least privilege: no shell, no fs, no network beyond fetch.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
