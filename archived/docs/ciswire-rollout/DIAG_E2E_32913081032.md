# DIAG E2E 32913081032

**Verdict: (B)** — pre-existing on `feat/moe-stream` (and on `main` after 2026-08-13). Not a CisWire empty-reply regression. Not a flake.

**Confidence: high (90%).** Twin emulator run `32908788071` (`c9047ba`, before `7c8cce7`) failed identically. Screenshots show a completed assistant turn the harness never read.

---

## Run

| | |
|---|---|
| Run | [32913081032](https://github.com/Aspis0/kalsa/actions/runs/32913081032) |
| Branch / SHA | `feat/moe-stream` / `7c8cce7` |
| Inputs | qwen3.5-2b, `COMPACTION=on` → anchored, `THINKING=off` |
| Job | e2e failed at **Run E2E on emulator** (00:17:59–00:46:16Z) |
| Surface fail | `wait_ui_idle` soft-timeout 240s, then `elapsed_to_reply_s=1434`, `reply<<< >>>`, `FAIL: no assistant reply captured` |

---

## 1. What the app actually did (1434s is a lie)

Emulator booted. APK + 1.28 GiB GGUF sideloaded. UI rendered, composer accepted `CiaoChiSei`, Send tapped (`sent at 1787703721` / 00:22:01Z). Engine ran.

**Telemetry:** none. Prefs: `kalsa.telemetry.state.A|{"v":1,"enabled":false,...`. Logcat dump is `adb logcat -d | grep RNLlama|llama|ReactNativeJS | tail -80` — only `pressure.transition` every 15s (`availableMb` ~5700–5800). No load lines, no token lines, no exception. Buffer started at 00:26:04 (load/send already rotated out). `kvdiag_t1.txt` empty.

**Prefill did start** (`reuse_t1.txt`):

```
Input processed: n_past=0, embd.size=1510
Input processed: n_past=1505, embd.size=1519
```

~3024 prompt tokens on turn 1 (last green turn1: **1108**, one chunk).

**Tokens streamed.** Screenshots, not SQL:

| t | file | UI |
|---|---|---|
| +20s | `03_sent` / `poll_1` | status **Writing**, stop square, no assistant text |
| +164s | `poll_8` | status **Thinking**, stop square, still empty |
| +183s | `poll_9` | thinking tokens: `The user is asking "CiaoChiSei" which is Italian for "Hello, what are you?"…` |
| +237s | `poll_11` | long think + visible reply starting `I'm Kalsa…` + cursor `\|` |
| **+254s** | **`poll_12`** | **turn done:** `Regenerate` + Copy/Read aloud. Visible reply: `I'm Kalsa, a private AI assistant running entirely on this device…` |
| +1434s | `99_final` | same completed bubble; composer is **send arrow** (not stop). Header still says `Thinking` (collapsed reasoning chevron) |

Quoted CI lines after that (harness never noticed):

```
[ci] poll 12: still generating (254s)
…
[ci] poll 60: still generating (1090s)
[ci] WARN: wait_ui_idle timed out after 240s — continuing (soft-fail; dump → turnend_timeout_ui.txt)
[ci] kv_reuse turn1: Input processed: n_past=0, embd.size=1510Input processed: n_past=1505, embd.size=1519
elapsed_to_reply_s=1434
reply<<<
>>>
[ci] FAIL: no assistant reply captured
```

`elapsed_to_reply_s=1434` = 60×15s polls against the **wrong key** + 240s `wait_ui_idle`. Wall time to a finished reply: **~254s**.

`history_*.json` / `history_final.json` are 0–1 bytes. Poll loop (`scripts/ci-e2e.sh`):

```
HIST=$(sql "SELECT … WHERE key='kalsa.messages.v1';")
if echo "$HIST" | grep -q '"role":"assistant"'; then
```

App writes `kalsa.messages.<conv-id>` (`ConversationsStore.messagesKey`). Prefs dump has `kalsa.conversations.v1|{"activeId":"conv-1787703637971-0qw7dshj`. Legacy `kalsa.messages.v1` is empty after migrate (`migrateLegacyIfNeeded` leaves it as recover-only).

`wait_ui_idle` then matches the leftover whole-node label `Thinking` as in-flight for the full 240s cap. Last green (31419281885) had the **same** `wait_ui_idle` timeout — it still PASSed because `kalsa.messages.v1` had the assistant row.

---

## 2. Last green vs 7c8cce7

Last success: **31419281885** 2026-08-10 `main` `43f5958` “engine: close six holes in the n_threads rule…”.

```
[ci] kv_reuse turn1: Input processed: n_past=0, embd.size=1108
elapsed_to_reply_s=524
[ci] PASS: real on-device inference completed (3 turns + …)
```

Same model, compaction=on, thinking=off. Distance: **331 commits** `43f5958..7c8cce7`. Last green **predates** `d710ace` (2026-08-24, kalsallama kernel). `43f5958..d710ace` = 325 commits.

`d710ace..7c8cce7` (6):

```
e171312  kalsallama pin a0cabca6b
510157d  CisWire lands
be52606  CisWire rollout docs
661942c  Fix CI harness regressions from CisWire rollout
c9047ba  Part-3 campaign kit rev 4
7c8cce7  Part-3 harness (bench.yml / ci-bench.sh / ci-lib.sh — NOT ci-e2e.sh)
```

---

## 3. Any E2E between d710ace and today?

On `feat/moe-stream`, **only these three** (all 2026-08-25, all failure):

| run | sha | failed step |
|---|---|---|
| 32905890087 | `be52606` | Typecheck + logic harnesses (never reached emulator; why `661942c` exists) |
| 32908788071 | `c9047ba` | emulator — **same** `embd.size=1510/1519`, `elapsed_to_reply_s=1435`, empty `reply<<<`, `FAIL: no assistant reply captured` |
| 32913081032 | `7c8cce7` | this run |

No green after `d710ace`. On `main` after last green, listed E2E runs fail at typecheck or were cancelled — **no emulator success after 2026-08-10**. E2E never ran against kalsallama + CisWire with a harness that can see the reply.

---

## 4. CisWire touchpoints (empty text vs slow)

Could 510157d/be52606/661942c/7c8cce7 make assistant text **empty**?

| Path | What I read | Empty text? |
|---|---|---|
| AppShell send → `streamAssistantTurn` → `onDone` → persist | `onDone` arms extract + `finish()`; save is chat-store to `kalsa.messages.<id>` | No. UI had text + `Regenerate` at poll 12 |
| `ciswireFlags` | bitmask telemetry only (`bit0=ciswire compaction, bit1=memory, bit2=toolhelp`). E2E compaction is **anchored** (`kalsa.context.compaction=1`), not `ciswire`. Memory default off. Toolhelp default off. Flags = 0 | No gating on tokens |
| `memoryFacts` tail, empty facts | `buildMemoryFactsBlock`: `if (!bounded) return ""`. `memoryEnabled` false → `promptFacts=[]` | No inject |
| `toolhelp` | `parseCiswireToolHelp(null)` → false. Not in E2E prefs | Off |

`7c8cce7` does not touch `ci-e2e.sh`. Twin fail on `c9047ba` already had 510157d.

Prompt grew 1108 → ~3024 on turn 1 (331 commits of tools/template/kernel, **plus thinking actually on** — see below). That is slowness/size, not empty completion. 1434s was capture spin, not hung decode.

**`THINKING=off` is a no-op** since `3875132` (2026-08-21, pre-CisWire):

```ts
// src/bench/benchConfig.ts — "off" is not in THINKING_MODES
// stored off falls back to default
```

Test: `getThinkingMode()` with `"off"` → `"default"`. Qwen 3.5 2B therefore **thinks**. That is why the `Thinking` header stays after `onDone` and why `wait_ui_idle` false-positives.

---

## 5. Class

| Class | Result |
|---|---|
| **(A)** CisWire regression 510157d/be52606/661942c/7c8cce7 | **REFUTED.** Reply existed. Same fail on `c9047ba` (pre-`7c8cce7`). Flags/facts/toolhelp cannot empty the bubble. |
| **(B)** Pre-existing on feat/moe-stream | **CONFIRMED.** Capture still reads `kalsa.messages.v1`; multi-chat `d3cf549` (2026-08-13) moved persistence to `kalsa.messages.<id>` — **after** last green, **before** CisWire. Thinking-off retired `3875132` (2026-08-21). Kernel `d710ace` never had a green E2E. |
| **(C)** Flake / emulator env | **REFUTED.** Two consecutive emulator arms, same SHA-family, same `n_past`/`embd` pair, same 1434–1435s empty capture. Boot, sideload, RAM (~5.7 GiB free) all healthy. |

---

## Recommended next action

Do **not** rerun the same workflow expecting green — it will fail the same way.

Minimal harness-only check (no CisWire change):

1. Point `scripts/ci-e2e.sh` polls at `messages_storage_key $(resolve_active_conversation_id …)` (helpers already in `ci-lib.sh`; `ci-bench.sh` already uses them).
2. Stop treating the post-turn `Thinking` label as in-flight (`wait_ui_idle` already documents this class of false positive; last green hit it too).
3. Treat `THINKING=off` as `default` unless a real disable arm exists. Do not use this run to A/B thinking-off.

Optional: one compaction=off arm after (1) if you want prompt-size/TTFT numbers vs last green 1108-token turn1. Not required to explain this FAIL.
