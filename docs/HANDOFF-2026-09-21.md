# Handoff — the interface rebuild, 2026-09-21

Written because this session's compaction was failing: `keepRecentTokens` was 20000, so pi tried
to summarise everything older than the last 20k tokens, and in a sixteen-hour session with huge
file reads that span was hundreds of thousands of tokens — the summary overran the model's output
ceiling and pi refused to write a mutilated one. The owner's fix, applied: `keepRecentTokens` is
now 200000 in `~/.pi/agent/settings.json`. This document exists so the work survives even if the
next session compacts badly too.

**Whoever picks this up: read `docs/DESIGN.md` and `docs/DESIGN-ROOMS.md` in full first.** They are
the plan and the honesty inventory, and they are the source of truth. This document is the
briefing on *state*, not a replacement for them.

## The task

Finish building the new Kalsa Android interface (`/Users/marco/Projects/kalsa-ux`, branch
`ux-2026-09-21`) and act as its orchestrator: launch agents using the configured paseo profiles,
verify their claims yourself, commit, and report to Marco in Italian.

## Context

- `kalsa-ux` is a **git worktree** of `/Users/marco/Projects/kalsa`. `REPOS.md` maps the five repos.
  The two living documents are in **`~/Projects/kalsa-moe-experiments`**: `PLAN.md`, `docs/ALIVE.md`.
- The interface was rebuilt from scratch; three assets survived: **the green palette, the
  empty-state photograph, the logo**. The engine, the governor, the session/KV code, the pins and
  the Meter are **not ours** — the new interface calls the same services.
- The desktop brain's settled shape is `kalsa-brain/docs/PLAN-CHAT-ON-DISK.md` (v3: one slot per
  device, non-unified). `kalsa-brain/docs/HOUSEHOLD-RULES.md` §4–§5 designs the shared room;
  `WHAT-IS-MISSING.md` §10 says none of it is built.

## Current state — committed and green

`npx tsc --noEmit` clean, `npx jest --silent` → **106 suites / 1417 tests**, all passing. Recent
commits, oldest first: `6778713` (design layer + type layer + the cloud port), `8a2a26e` (step 1b +
the shell), `318488d` (three sizes from one build), `07cb7ce` (step 3, the transcript, the cloud on
screen), `8497933` (the cloud's `Easing` crash), `0b2d2a0`, `d1c2781` (logo, cloud face, cloud air,
the pin wired), `d9c02d3` (chrome, clearance, fixture, scroll grace), `907a538` (the keyboard inset
reaching the composer), `1f68393` (step 4: tool rows and source chips), `b3ea8c1` (the plan's
substituted proofs).

What exists and works:

| piece | where |
|---|---|
| The green palette, all contrasts measured, role constraints as tests | `src/theme/design.ts`, `design.test.ts` |
| Inter + Source Serif 4 + Plex Mono; 10 faces loaded, 9 referenced by a role | `src/theme/{fonts,typography,typeData}.ts`, `typography.test.ts` |
| The thought cloud, ported, 50 tests, with a worklet import guard | `src/ui/thinking/` |
| The three bands, the geometry, the styles, the keyboard hook | `src/ui/shell/{shellGeometry,shellStyles,Shell,useKeyboardHeight}.tsx` |
| The transcript: capsule, bare answer, day marker, pin machine, evidence | `src/ui/shell/{transcriptLayout,transcriptScroll,Transcript,TranscriptParts,transcriptTypes,TranscriptEvidence}.tsx` |
| Tool labels and the source-chip policy, both pure and tested | `src/ui/shell/{toolLabels,sourceLinkPolicy}.ts` |
| The preview: renders the shell alone, cycles sizes on the strip's `+` | `src/ui/shell/ShellPreview.tsx`, `App.tsx:52` `SHELL_PREVIEW` |

Verified on a device, not assumed: the composer's bottom edge sits exactly on the IME's top edge
(498 px = 498 px, two independent measurements); the logo renders; the cloud animates; the day
marker appears.

## The queue, in priority order

1. **The capture is done, and a vision audit has read it.** The four PNGs are in `mock/`
   (`shell-jelly-621.png`, `-keyboard.png`, `-notice.png`, `-cloud.png`, 480×854). Verified on the
   device, not assumed: build 36 s with **zero** C++ compilers, the installed APK byte-identical to
   the built one, composer bottom 498 px = IME top 498 px (gap 0, PASS), end-of-content clearance
   exactly 24.0 dp = `TRANSCRIPT_LAST_ITEM_GAP`, scales restored to `1 1 1`. The audit found three
   things the measurements could not: the floating **"Go to the end"** control covering the reader's
   own word "trust?" (**fixed, `76e9d5d`**), the thinking card cut flat at the band's top edge, and
   a "cloud" that reads as one grey dot — which turned out **not** to be a defect (§2.3: the element
   is a card with a ticker and a trail of three bubbles, in the desktop too).
2. **The chip's 48 dp touch box** — **done, `7b81f11`**. The painted chip stays ~28 dp and the
   pressable is a real 48 dp box on both axes (`SOURCE_CHIP_TOUCH_BOX` beside the band constants,
   `SOURCE_CHIP_BOX_COST` = 20 dp named rather than discovered). Proved against the **literal 48**, so
   lowering the project's floor cannot lower this box, plus a source check that the two chip nodes
   wear it. **Not yet photographed**: the four PNGs predate it.
3. **Step 3b, markdown** — **built, `1a4ed91`, its pixels pending.** Tables, fenced code, and the
   inline set the shell never had (bold, italic, inline code, links, `[N]` chips). Parsing is
   `src/chat/markdownDocument.ts` (new pure module beside `markdown.ts`, which changed by one
   comment); rendering is `TranscriptMarkdown.tsx` + `TranscriptInline.tsx` with design-layer colours.
   `tableScrollDecision` still decides scrolling, and a table or code box carries **no** accessibility
   label on purpose (a `contentDescription` on a container hides its cells from a screen reader).
   110 suites / 1461 tests. **The capture has not happened**: it needs the emulator, and the machine is
   shared. What it must show, from the coder's report: at 349 dp the pinned frame carrying the preview
   answer (heading, two bullets, a blockquote, a **three-column table that must scroll** — cell 351 dp
   needed against a 321 dp column — and a mono code block with its `json` label row), **plus a second
   frame after one horizontal swipe inside the table** proving the third column is scrolled to rather
   than lost; then the 325 dp keyboard case; then 360×780. No capture can prove the partial boundaries
   — the prefix walk does that. **The owner cut display equations** (2026-09-21) after the engine's own
   system prompt was read: `src/i18n/en.ts:1158` asks only for short paragraphs and bullet lists, and
   the only fence it mentions is a ```json one for the miniapp fallback.

   **Three questions the owner owns, raised by the coder and not decided by it:** the inline `[N]`
   marker is **not tappable** (the old renderer opened the source on a tap; the same source is a 48 dp
   chip under the answer, and a 48 dp box inside a sentence would push the lines apart);
   `splitParagraphs` (`transcriptLayout.ts:185`) is now **test-only**, superseded by the parser; and
   `ESCAPABLE` in `markdown.ts:221` omits a bare `|`, so prose `\|` keeps its backslash.
4. **The mount** — the shell is not in the running app; `ShellPreview` is its only consumer. Mounting
   is a host step and it is what un-drops the tool names (the old UI still throws them away at
   `AiChatPage.tsx:2623-2651`) and what settles the persistence asymmetry (sources persist,
   tool rows deliberately do not).
5. **Plan steps 5–11**: the composer's held states, stop's four outcomes, mini apps (39 block kinds),
   local vs remote (blocked on `remote-brain`, which is 408 commits away), permissions in one place,
   the settings split, the accessibility sweep.
6. **Extend `scripts/ci-screens.sh`'s "composer bottom ≤ IME top" assertion to the new shell** — it
   exists for the old app at `:647, :690-693` and it is the only check in the project that fails when
   the keyboard breaks.
7. **Dead code awaiting the owner's word**: `src/theme/components/Surface.tsx` and `Button.tsx` are
   imported by nothing and carry the old palette's orange shadow `#f07a3f`. Proposed for deletion,
   not deleted.
8. **The room** (`docs/DESIGN-ROOMS.md`): deferred by the owner until the desktop session finishes.
   It has 19 dependency rows, each with the file that decides it.

## Decisions that must not be silently reversed

- The palette is **green**; the dead `notebook` palette was deleted.
- **The transcript grows from the top while it fits and sticks to the bottom once it overflows.** A
  short conversation keeps its empty space — the owner chose the desktop behaviour explicitly.
- **One list entry per message**, created when an answer starts and never re-created, because a
  placeholder born in one place and the real text arriving elsewhere is how the
  writes-above-then-below bug starts.
- **The keyboard is an inset, not a resize**: the bands re-partition, and lifting the whole shell
  cannot work (it would push the strip off the top).
- **Tool rows are volatile and never persisted**; sources persist. The durable evidence of what an
  answer stands on is the sources.
- **Source chips have no favicons and make no network request** — a favicon would leak the domains
  the user searched. Proved by a source check, not by a promise.
- **Numbers that are chosen are labelled chosen** in the code, never dressed as derived ones.
- **The proof regime**: there is no render harness (`testEnvironment: node`, `testMatch
  **/*.test.ts`, no `react-test-renderer`, no testing-library). Arithmetic and decisions live in
  pure modules with tests; pixels are proved by screenshots; source-level rules are proved by
  checks that read the files and prove themselves non-vacuous.

## Constraints — the hard rules

- **Never push. Never merge to `main`.** No new branch, no new worktree. `git add` with explicit
  paths only, never `-A`. An audit (the Reviewer profile) before anything is pushed.
- **UI only**: never touch the engine, the governor, the session/KV, the engine pins or the Meter.
- **Phones**: the Jelly Star is `192.168.1.82:5555` — install only when the coordinator
  (`155f166b`) says the phone is free, `adb install -r` only, never uninstall, and report the
  sha256 left on it. **The Galaxy S23 (`192.168.1.152:43089`) is off-limits entirely.**
- **Emulator: one at a time.** Boot with `-no-snapshot-load -no-boot-anim -no-audio -no-window
  -crash-report-mode disabled -port 5554` (without the crash flag it hangs at 0 % CPU on a stale
  record). Keep the three animation scales at `1` — reduce-motion pins the cloud to its rest pose and
  so hides the *motion*. (Read against the source, reduce-motion makes the trail **more** visible —
  the rest pose is `opacity 1` — and React Native reads it from `transition_animation_scale`, not
  from `animator_duration_scale`. See `docs/DESIGN.md` §2.3.)
- **No still frame can prove the thought cloud.** Its whole gesture is three rings ≤ 9 dp travelling
  14 px. The evidence is a **pair of captures a fraction of a period apart** (~0.6 s of the fixture's
  2.4 s turn), plus the leading ring's measured diameter as the second witness. The fixture hands the
  cloud **one** arrival at mount, so its period sits at the 2.4 s clamp and never changes; the phase
  is pinned to `rise` forever, so there is no “right moment” to catch and no earlier-is-better rule.
- **The build costs 36–46 s and must compile zero C++**: `./gradlew assembleRelease --console=plain
  --no-daemon --max-workers=2 -PreactNativeArchitectures=arm64-v8a -PrnllamaBuildFromSource=false`.
- All UI text through `t()` in **both** `src/i18n/en.ts` and `src/i18n/it.ts` (`it.ts` is typed
  `typeof en`, so a missing key is a compile error). **English everywhere** — code, comments, docs,
  commit messages; Italian only inside `it.ts` values and when speaking to Marco.
- Files ≤ ~350 lines. Comments only for a WHY or a trap. Real boxes ≥48 dp, never `hitSlop`. Never a
  numeric `fontWeight` beside a custom family. `testID` + an accessible name on every interactive
  node.
- The suite stays green; **never delete or weaken a test to go green** — report every replacement.
- **Use the configured paseo profiles, materialising every field** (ids from `list_profiles`):
  Explorer `agent_profile_pubvia_explorer`; Skilled Coder `agent_profile_mu1ad3a6_m2izc4n41n`;
  Reviewer `agent_profile_mswefir2_65xu7ff5vy2` (modeId `edit`, thinkingOptionId `high`,
  featureValues `{auto_accept: true}`); Websearch `agent_profile_pubvia_websearch`;
  Free Coder open `agent_profile_mt67yab0_cxc05sgx8nf`. Never invent a provider.
- Workspace `wks_971bdacae1f38423`. The coordinator is `155f166b`.

## What was tried and failed — do not repeat these

- **An audit in `plan` mode produced nothing**: the auditor could not run bash, so it could not
  recompute anything. Audits must be able to execute.
- **Auditing a moving tree produced ghosts.** Commit first, then audit the frozen sha.
- **A Reviewer launched without `auto_accept: true`** interrupts with a permission request per
  command. Materialise every field of the profile.
- **`--max-workers=2` does not cap the NDK**: AGP invokes `ninja -C <dir> <targets>` with no `-j`,
  and the CI recipe's `CMAKE_BUILD_PARALLEL_LEVEL`/`NINJAFLAGS` are no-ops for the same reason. 21
  concurrent `clang` flattened the machine. The fix is not a cap: **turn the C++ build off** with
  `-PrnllamaBuildFromSource=false` (a CLI `-P` outranks `android/gradle.properties:65`, which the
  Expo plugin sets to true). `KALSA_LLAMA_FROM_SOURCE=0` throws at prebuild, so it can only turn it
  on; `-PrnllamaHexagon=false` has no consumers and is decorative.
- **Two emulators at once flatten the Mac.** The owner's words: "un emulatore per volta".
- **`git checkout -- <file>` on an uncommitted fix reverts the fix.** Copy to `/tmp` first.
- **A tap zone under the status bar is unreachable** — the status bar's touchable region sits above
  the app window. The preview's size cycle is on the strip's `+` instead.
- **`uiautomator dump` cannot run while the cloud animates** ("could not get idle state"): read
  bounds with animations off, restore them, then capture.

## A practical warning about this repository

**Reading whole files is what killed this session's context.** `AiChatPage.tsx` and `AppShell.tsx`
are enormous (the first is over 7000 lines). Always read with `offset`/`limit`, prefer `grep -n`
with a narrow pattern, and let an Explorer agent do the wide reading. The design documents are the
only files worth reading whole.

## Acceptance criteria for the immediate next step

- [ ] The capture agent's four PNGs exist in `mock/`, and someone with vision has said what is
      actually visible in them, including the tool rows, the chips, the notice line and any defect.
- [ ] The chip's pressable box is a real 48 dp box while the painted chip stays small, with the
      constant declared beside the other band constants and a test that fails if it shrinks.
- [ ] `npx tsc --noEmit` and `npx jest --silent` green, with the suite count reported.
