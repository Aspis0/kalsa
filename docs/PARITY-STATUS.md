# PARITY-STATUS — the answer to `docs/PARITY.md` at commit `3777479`

This file is the row-by-row answer to the specification in `docs/PARITY.md`, verified on
branch `ux-2026-09-21` at commit `3777479` (clean worktree at time of walk).
Method: read-only walk of `src/host/**`, `src/ui/shell/**`, `App.tsx` against the two
controller files by grep + bounded reads; no test was executed (existence claims only).
**Re-walk and regenerate this file after any slice that touches `src/host/**` or
`src/ui/shell/**`** — every verdict below is only true of the tree that produced it.

Vocabulary (D1): IMPLEMENTED · STUBBED (held, with the toast key or hold line that says
why — §2.7) · MISSING · DIFFERENT (with the how). Shorthand: `App` = `src/app/AppShell.tsx`,
`Chat` = `src/screens/AiChatPage.tsx`.

---

## Table 1 — Deliverable 1: the 50 feature rows

| # | Feature | Verdict | New location / hold signal (and the how) |
|---|---|---|---|
| 1 | Drawer: list, tap-switch, long-press delete, new-chat, persona row, 180 ms search, keyboard dismiss per row | **DIFFERENT** | List/tap: `src/host/conversationActions.ts:248-260` (long-press → confirm `:258` → dialog `:221-230`); new-chat `:119-172`; persona row `src/host/HostRoot.tsx:287-296`; debounce `src/host/useConversationHost.ts:23,51-62`; static-item dismiss `conversationActions.ts:269,281,292,303`. **How different:** the persona row press no longer dismisses the keyboard (old `AppShell.tsx:7104`; new `HostRoot.tsx:292-296` has no `Keyboard.dismiss`). Conversation rows never dismissed in the old app either (old dismiss sites are only `AppShell.tsx:2576,2588,2599,2610` = the four static items) — that part matches. |
| 2 | Chat header: menu, export/share, new chat | **DIFFERENT** | Menu `src/ui/shell/Shell.tsx:168-176` → `HostRoot.tsx:253`; new chat `Shell.tsx:210-218` → `HostRoot.tsx:255`. Export/share absent: no Share/export symbol anywhere in `src/ui/shell/**` or `src/host/**` (old `Chat:3206-3218`, button `Chat:4757-4769`). |
| 3 | Eight exclusive overlays | **DIFFERENT** | 7 of 8 kinds: `src/host/hostOverlay.ts:4-15`, mounts `src/host/HostOverlays.tsx:149-263` (Settings `:155`, Account `:206`, Pro `:213`, Documents `:215-231`, Notes `:234-243` with `focusId`, Personas `:245-255`, Help `:257-263`). **How different:** the `miniapp` kind is deleted, with a written report at `hostOverlay.ts:7-16`. |
| 4 | Mini-app card **and** full-screen sheet | **MISSING** | No miniapp field in the transcript shape (`src/ui/shell/transcriptTypes.ts:36-56`), no mapper read (`src/host/messageMapper.ts:69-77`), no overlay kind (`hostOverlay.ts:7-16`), renderer `src/ui/AskAssistantMiniappRenderer.tsx` never mounted. Plumbing survives: persist `src/host/historyMessages.ts:120-131`, capture `src/host/sendCallbacks.ts:117-125`, hook `HostRoot.tsx:70` (never has an opener). Old `Chat:6033-6093`, `App:7214-7252`. |
| 5 | Web switch in the top bar (persisted) | **MISSING** | No `toggleWebTools` exists in the new tree — stated at `src/host/toolFlags.ts:6-11`. Flag still loads default ON (`toolFlags.ts:31-44`) and still reaches the tool gate (`src/host/agentTurnOptions.ts:209`) and the static-prefix notifier, so behavior defaults to old-ON; the control is gone. Old `App:6926-6959`. |
| 6 | Composer field, held placeholders (§2.7) | **DIFFERENT** | Field + editable/placeholder: `Shell.tsx:266-277`; hold reason row `Shell.tsx:241-248`; decisions `src/ui/shell/composerState.ts:158-175` fed by `src/host/composerView.ts:58-63` → `HostRoot.tsx:246-248`. The old `editable`+placeholder bug is unrepresentable (`composerState.ts` `FieldView` union). **How different:** tap-transcript-to-focus and focus management do not exist (no `focus()`/`inputRef` anywhere in `src/host/**` or `src/ui/shell/**`; old `Chat:4329-4331`), and the KAV lift was replaced by keyboard-controller insets (`src/ui/shell/useKeyboardHeight.ts`, `Shell.tsx:137-140` — documented mechanism change at `Shell.tsx:8-16`). |
| 7 | Action row: templates ✦, attach, mic (3 voice states), send⇄stop, per-control disabled | **DIFFERENT** | Send/stop/stopping faces + disabled: `Shell.tsx:290-310` (disabled `:293-295`), faces from `composerState.ts:170-172`. Attach and mic are §2.7 **stubs**: press fires toast `shell.notice.attach` (`HostRoot.tsx:256`) and `shell.notice.mic` (`HostRoot.tsx:257`), copy at `src/i18n/en.ts:1237-1238`. **Absent:** templates ✦ entry, mic's three voice states, voice/PDF busy rules. Note: PARITY's "composerState has zero importers" is **stale** — it is now mounted via `composerView.ts:10,58`. |
| 8 | One `canSend` boolean | **IMPLEMENTED** | `composerState.ts:171` (`canSend: rule.hold === null`), consumed as `sendEnabled` at `composerView.ts:61` and dimming `Shell.tsx:293-295`. Caveat: only the inputs this build has (history/thermal/sending/stopping/model/draft); the attachments/translate/voice/pdf inputs left with systems this host does not mount. |
| 9 | Streaming caret after last segment | **MISSING** | `grep caret\|streaming` over `src/ui/shell/**` returns only comments; the mapper drops the streaming flag (`messageMapper.ts:69-77`); nothing renders mid-answer motion. Old `Chat:5484,5559-5576`. |
| 10 | Stop: abort, 3 s watchdog, interrupted marker, empty drop, thermal note, failure row | **DIFFERENT** | Machinery live: `src/host/sendStop.ts:43-96` (3 s timer `:96`, retire-first `:62`, drop-empty `:67`, mark `:75`, persist `:79`, unlock-own-only `:83-95`); abort-before-token rollback `src/host/sendHost.ts:264-271`; thermal hold line via phase `src/host/composerPhase.ts:34-49` → `shell.held.tooHot` → `Shell.tsx:241-248`; failure text `sendHost.ts:224` + `src/host/engineTurnStream.ts:279` (⚠️ as message text). **How different:** the interrupted marker on the partial is never drawn (mapper carries no `interrupted` field, `messageMapper.ts:69-77`; Transcript has no marker — old `Chat:5627-5630`), and `stopOutcome` (`composerState.ts:228-270`) has **zero importers** — the four-outcome stop view is decided but unrendered. |
| 11 | Send path: fit gate → claim → stream → turn-end save | **DIFFERENT** | Claim `sendHost.ts:125,131`; content gate `:139-158`; append/stream `:161-262` → `src/host/engineTurn.ts:53-331`; turn-end save `src/host/sendFinalize.ts:59-170`. **How different (all reported at `sendHost.ts:8-15`):** the chat-side pre-send fit gate is not lifted (fit now only on the load path, `src/host/engineLoad.ts:104`); the bench-command branch (old `Chat:2317-2330`, `/bench …`/`bench:…`) and the doc-hint composition are absent; result classification moved into `src/host/sendStream.ts:91-148` (tested). |
| 12 | Empty state: art, greeting, welcome, 4 suggestions | **MISSING** | `Transcript` renders messages only (`src/ui/shell/Transcript.tsx:198-247`); no raster/greeting/suggestions symbol in `src/host/**` or `src/ui/shell/**`. Old `Chat:191-194, 4015-4131, 423-453`. First open of a new conversation is a blank band. |
| 13 | Quick templates sheet | **MISSING** | `grep QuickActionSheet` in `src/host/** src/ui/shell/**` = 0 (component `src/theme/components/QuickActionSheet.tsx` never called). Old `Chat:4375-4380`, entry `Chat:4821-4837`. |
| 14 | Research / library-doc / notes chips + auto-clear | **MISSING** | `grep researchMode\|notesMode` = 0; `composerView.ts:58` passes `attachment: null` unconditionally. Deep-research survives only as a typed-text trigger (`sendHost.ts:161-163`, engine branch `engineTurn.ts:186`); the notes branch (`engineTurn.ts:246-257`) is unreachable because no UI ever sets `options.notes` (`sendHost.ts:244` sets research only). Old chips `Chat:4189-4221`. |
| 15 | Long-press (350 ms) message menu | **MISSING** | No `onLongPress` in `src/ui/shell/**`; the only one in the new tree is the drawer's `conversationActions.ts:258`. Old modal `Chat:4385-4496`, openers `Chat:5332-5526`. |
| 16 | Inline chips: copy, read-aloud, "more", copied flash | **MISSING** | `grep Clipboard\|speakingId\|copiedFlash` in `src/host/** src/ui/shell/**` = 0. Old `Chat:5419-5434, 5593-5624`. |
| 17 | Edit-then-resend modal | **MISSING** | `grep editMessage` in `src/host/**` = 0. Old `Chat:4500-4581, 3329-3465`. |
| 18 | Translate a message | **MISSING** | `grep runTranslate\|translateAbort` in `src/host/**` = 0. Old `Chat:3533-3582`, orphan cleanup `Chat:1951-1957`. |
| 19 | Read aloud / stop reading | **MISSING** | `grep handleReadAloud\|speak` = 0. Old `Chat:1613-1660`. |
| 20 | Save message to notes + notice | **MISSING** | `grep handleSaveToNotes\|saveNote(` = 0 in `src/host/**` (`src/notes/NotesStore.ts` untouched and un-called from the new root). Old `App:3749-3763`. |
| 21 | Regenerate from a target turn | **MISSING** | No UI entry; `regenState` locks exist only to be cleared (`sendHost.ts:96-108`, `useHostEffects.ts:124-127`); `src/screens/regenTarget.ts` never imported by `src/host/**`. Old menu `Chat:4468-4486`. |
| 22 | Source chips under an answer | **IMPLEMENTED** | Mapper `messageMapper.ts:70-74`; safe-URL policy `src/ui/shell/sourceLinkPolicy.ts:165`; render + system-browser tap `src/ui/shell/TranscriptEvidence.tsx:65-151`; tests `sourceChipBox.test.ts`, `sourceLinkPolicy.test.ts`, `transcriptNoFetch.test.ts`. Representation differs (index + host instead of provider colours/title — old `Chat:5213-5225` `PROVIDER_COLORS` unused); PARITY's "host mapper pending" is now done. |
| 23 | Tool rows (volatile) | **IMPLEMENTED** | The step PARITY awaited is landed: capture branch `sendCallbacks.ts:84-89` (payload decoder `messageMapper.ts:98-108`), volatile map `HostRoot.tsx:150-155`, cleared per conversation (`HostRoot.tsx:117-120`, `useHostEffects.ts` clearTools), render `TranscriptEvidence.tsx:39-60`, label fallback `src/ui/shell/toolLabels.ts:68`. |
| 24 | Thinking status chip + status history | **IMPLEMENTED** (different representation, as PARITY anticipated) | Live cloud: `messageMapper.ts:54-67` → `ThoughtCloud` via `TranscriptParts.tsx:66-73`; status label/history still written per callback (`sendCallbacks.ts:70-79`) and volatile by decision (`historyMessages.ts:104-107`). |
| 25 | Error rows: engine reason, content filter, interrupted | **DIFFERENT** | Engine reason as ⚠️ message text `engineTurnStream.ts:279` + `engineTurn.ts:115-124`; content-filter line `sendHost.ts:36-53,139-158` (old `Chat:532-558`). **How different:** the interrupted line is never rendered (see row 10) — 2 of 3 rows exist. |
| 26 | CTA buttons on answers | **MISSING** (user-visible) | Captured (`sendCallbacks.ts:84-115`) and persisted (`historyMessages.ts:146-161`), but `TranscriptMessage` has no `ctas` field and nothing draws them. Old render `Chat:5764-5790` (old press was a no-op too, `App:7047`). |
| 27 | Day divider | **IMPLEMENTED** | `src/ui/shell/transcriptLayout.ts:156-164` (`shouldShowDayMarker`, floor `:60`), render `Transcript.tsx:205-231`, tested `transcriptLayout.test.ts`. |
| 28 | Mini-app card inside a message | **MISSING** | Same as row 4: no card renderer; `MiniappCard` old `Chat:6033-6093`. |
| 29 | Mic: listen → transcribe → prefill | **STUBBED** | Chrome `Shell.tsx:280-287`; press fires toast **`shell.notice.mic`** = "Dictation is not available in this build yet." (`HostRoot.tsx:257`, copy `en.ts:1238`). No voice pipeline (`grep voiceRunId` in `src/host/**` = 0). Old `Chat:1515-1611`. |
| 30 | Voice status row + voice-note toast | **MISSING** | No voice UI symbol anywhere. Old `Chat:4299-4314`. |
| 31 | Whisper download, TTS toggle, voice-ready gate | **STUBBED** (mixed) | Presence scan live `src/host/usePipelineScans.ts:176-190` (`isWhisperModelDownloaded` `:178`); TTS toggle real (`usePipelineScans.ts:83-88` → `HostOverlays.tsx:191`); download held → toast **`shell.notice.voiceDownload`** (`HostOverlays.tsx:190`, copy `en.ts:1240`). Old download UI `App:5155-5295`. |
| 32 | Voice cleanup on unmount/conversation change | **MISSING** | No voice timers/capture exist to clean (old `Chat:1412-1426`). Unmount/conversation effects at `useHostEffects.ts:72-99,104-118` handle only abort/flush/tools. |
| 33 | Top-bar model chip: name · quant · status + tap semantics | **DIFFERENT** | Pill draws name + "On this phone" only (`Shell.tsx:178-206`, labels `HostRoot.tsx:241-244`). Tap semantics reproduced at `HostRoot.tsx:221-233`: hung → inert (`:222`), missing/download-error → toast **`shell.notice.download`** (`:225`), checking/loading/resident → no-op, else `userReloadModel`. **How different:** no quant/status/%, no "downloading" state (old `App:6873-6911, 6746-6788, 6720`), and missing→download is a toast instead of a download. |
| 34 | Download progress bar, %, notifications | **MISSING** | `downloadPercent: null` in all three Settings slots (`HostOverlays.tsx:170,185,195`); no `startDownload`/`notifyDownload` in `src/host/**`. Old `App:6971-6985`, notify `App:2625-2712`. |
| 35 | Error + hint lines under the bar | **DIFFERENT** | The hint builder is lifted but rendered **only inside Settings** (`HostOverlays.tsx:135-147`, passed `:172`); the strip has no error row (`Shell.tsx:162-217` draws menu/pill/newchat only). The composer does show `unloaded` on error (`composerPhase.ts:34-49` → hold line). Old render `App:6987-7008`. |
| 36 | Advisory battery ETA line | **MISSING** | `grep batteryEta` in `src/host/**` = 0. Old `App:2758-2830`, render `App:6921`. |
| 37 | Web on/off switch (row 5 dup) | **MISSING** | Same as row 5 (`toolFlags.ts:6-11`). |
| 38 | Memory banner (set-only, never rendered) | **MISSING** (deliberate, reported) | Dropped with a written report at `src/host/memoryHost.ts:6-8` and `src/host/engineEnsure.ts:14-18`; `grep memoryBannerKey` = old `AppShell.tsx:3021` + prose comments only. Owner decision already carried by PARITY (D1 row 38 / D2 row 18). |
| 39 | Notice toast (single slot, 4 s, bottom 96) | **IMPLEMENTED** | `src/host/useNotice.ts:19-24` (one state, one 4 s timer, last-write-wins), render `src/host/HostNotice.tsx:16-36` (`bottom: 96` `:22`), testID `host.notice`. Keys `en.ts:1236-1243`. |
| 40 | Voice-note toast (separate system) | **MISSING** | No second toast system (old `Chat:1329-1338`). |
| 41 | Share-in: prefill, file imports, notices | **MISSING** | No `shareIntent` import and no `Linking` event listener in `App.tsx` or `src/host/**`. Old `App:3553-3671`, `shareIntent.ts:37`. |
| 42 | History-guard Alerts | **IMPLEMENTED** | `src/host/useHistoryHost.ts:172-244`: begin `:172`, settle `:196`, throw-guard alert `:179`, preservation-failed `:206`, unreadable `:217`, partial-count `:227`, lossy flush `:235-244` (four alert sites vs old three — the throw path is new and reported in-code). |
| 43 | Attach / context chip rows | **MISSING** (shape only) | `attachment` is hard-wired `null` (`composerView.ts:58`); no `attachedItems` state; shape exists at `composerState.ts:64,174-175`, unrendered. Old `Chat:4222-4247`, state `Chat:1249-1252`. |
| 44 | Stick-to-bottom + auto-scroll | **IMPLEMENTED** (different mechanism, tested) | `src/ui/shell/transcriptScroll.ts:91-133` + `Transcript.tsx:104-155` + jump pill `Transcript.tsx:257-270`; tests `transcriptScroll.test.ts`, `transcriptJumpPill.test.ts`. |
| 45 | Keyboard dismissal on dismissible surfaces | **DIFFERENT** | 4 of the old 5 dismiss sites: `conversationActions.ts:269,281,292,303`. Persona row lost its dismiss (old `App:7104` → `HostRoot.tsx:292-296`). No `keyboardShouldPersistTaps="handled"` on the new transcript (actual old lines `Chat:4025,4146` — PARITY's 4029-4030 has line drift; `Transcript.tsx:163-189` is a plain ScrollView, default `never`). `grep Keyboard.dismiss` in `src/ui/shell/**` = 0. |
| 46 | Focus: after template choice, tap-to-focus | **MISSING** | No `focus()`/`inputRef` in `src/host/**` or `src/ui/shell/**`. Old `Chat:3637, 4329-4331`. |
| 47 | Long-press 350 ms + a11y on messages | **MISSING** | No long-press on any transcript element (`Transcript.tsx:198-247`, `TranscriptParts.tsx` pressables absent). Old `Chat:5333-5334,5396,5524`. |
| 48 | Haptics | **NOTHING TO PRESERVE** | Old grep empty (PARITY); new grep `haptic\|vibrat` over `src/host/** src/ui/shell/**` = 0. Parity holds. |
| 49 | Swipe-to-delete | **NOTHING TO PRESERVE** | New grep `swipe` = 0 in `src/host/** src/ui/shell/**`; delete is drawer long-press (`conversationActions.ts:258`). Matches old. |
| 50 | Keyboard-debug badge (dev) | **MISSING** | `grep kbDebug` in the new tree = 0. Old `Chat:865-904, 4668-4691`. Dev-only; keep/drop still an open decision (PARITY carries it as such). |

**Tally:** IMPLEMENTED 8 (8, 22, 23, 24, 27, 39, 42, 44) · DIFFERENT 11 (1, 2, 3, 6, 7, 10, 11, 25, 33, 35, 45) · STUBBED 2 (29, 31) · MISSING 27 (4, 5, 9, 12-21, 26, 28, 30, 32, 34, 36-38, 40, 41, 43, 46, 47, 50) · nothing-to-preserve 2 (48, 49). 8 + 11 + 2 + 27 + 2 = 50.

---

## Table 2 — Deliverable 2: the 21 state/plumbing rows

| # | Row | Verdict (new location) | Does the named check exist against the new code? |
|---|---|---|---|
| 1 | Epoch trio | **REPRODUCED** — checks before build and before write `src/host/historyWrite.ts:80,86`; load bump `src/host/useHistoryHost.ts:144`; delete bump injected `HostRoot.tsx:143` → called `conversationActions.ts:213`; switch flush-before-bump `conversationActions.ts:89` (flush lands before the state change that re-runs the load effect's bump). `flushThenBump` (`historyWrite.ts:95-103`) exists but has **no production caller** — see Section 4.3. | **Yes, replaced.** Test exists: `src/host/historyWrite.test.ts:78,91` (stale epoch → no build/no write), `:107` (flush before bump), `:119,131` (bump before delete). Grep changed: `getEpoch() !== opts.epoch` (old `Chat:619,629`) → **`epoch !== stamped` = 2 sites, both `src/host/historyWrite.ts:80,86`**. |
| 2 | Hash symmetry (7 old callers; 3 raw) | **REPRODUCED at 5 sites** — `useHistoryFlushes.ts:120-127` (AppState, persistable), `sendFinalize.ts:151-156` (turn-end, persistable), `modelSwitch.ts:153-159` (raw `readBootMessages`), `conversationActions.ts:98-105` and `:158-165` (raw ×2). Raw readers still exactly 3 (old `App:2391,2453,4536`). **Lost:** old `Chat:2161` (background-discard snapshot hash) has no counterpart — the background lifecycle is not mounted; old's two turn-end saves (`Chat:2981,3054`) folded into one finalize path. | Frozen-hash test **exists** against the shared engine file: `src/engine/sessionPersistence.test.ts:179`. Grep rule holds: no `src/host/**` caller hashes a live `Message[]` — persistable side goes through `buildPersistableMessages` (`src/host/historyMessages.ts:37-41` → `toPersistableHistoryMessages`), raw side through `readBootMessages`. No new-side hash test. |
| 3 | `updateMessage` guards (9 sites) | **REPRODUCED** as the turn fence: run patches are `fence.apply`-wrapped at `sendCallbacks.ts:42` (covers all rich callbacks), `sendHost.ts:144,164,186,268`, `sendStop.ts:65`, `sendFinalize.ts:76`; load-path resets `useHistoryHost.ts:153,192` are epoch-bump-fenced by `:144`. `updateMessage` does not exist in `src/host/**`. | **Replaced.** Test: `src/host/turnGuards.test.ts:4-55` (stale run/gen, refused update changes nothing, deferred-updater re-check). Old grep (`updateMessage(` count == `myGen` count) → new: **`grep "fence\.(apply\|owns\|beginRun\|invalidate\|retire)" src/host` = 27 non-test sites** (`sendCallbacks.ts` 11, `sendHost.ts` 8, `sendFinalize.ts` 3, `sendStop.ts` 3, `useHostEffects.ts` 2). |
| 4 | Run-id + generation lifecycle | **REPRODUCED** as fence ops: `beginRun` `sendHost.ts:131`; `invalidate` `useHostEffects.ts:81,111`; `retire` `sendStop.ts:62`; regenState module locks still honored (`sendHost.ts:96-108`, `useHostEffects.ts:124-127`). | Partly. Test exists for retire ordering: `turnGuards.test.ts:61-70`. **The named watchdog test ("abort never settles → 3 s unlocks only its own run") does not exist** — no fake-timer watchdog test in `src/host/**`. Old greps dead: `regenGenerationRef.current +=` → **0 in `src/host/**`** (4 sites old, `Chat:1867,1929,3162,3264`); `++sendRunIdRef` → **0** (1 site old, `Chat:2308`). Replacement counts: `fence.beginRun()` 1, `fence.invalidate()` 2, `fence.retire()` 1. |
| 5 | Abort path (7 old sites) | **REPRODUCED (3 of 7)** — stop `src/host/sendStop.ts:46` (direct `controller.abort()` on the captured controller instead of the ref-optional call), conversation change `useHostEffects.ts:78`, unmount `useHostEffects.ts:109`. Not reproduced: background lifecycle (`Chat:2103,2134`), clearChat (`Chat:3230`), edit (`Chat:3359`) — all features not mounted. Ordering note: old aborted **before** flush on clear; new conversation-change abort runs **after** the flush and after commit (flush at `conversationActions.ts:89`, abort in the effect `useHostEffects.ts:78`). | Replaced. Old grep `abortRef.current?.abort()` (7) → **2 in `src/host/**` (`useHostEffects.ts:78,109`) + `sendStop.ts:46`**. The named identity-guard test has no direct form; its property is covered by `turnGuards.test.ts:4-28` (stale token never nulls newer state — the fence is the identity guard now). |
| 6 | Stop watchdog (3 s) | **REPRODUCED** — `sendStop.ts:47-96`: token match `:53-55`, retire first `:62`, drop-empty `:67`, mark `:75`, persist in updater `:79`, unlock own run only `:83-95`; drop-on-clear replaced by `fence.invalidate()` + watchdog clear in `useHostEffects.ts:81-89`. | **No.** The named test (watchdog fires after bump → no state change) does not exist. Grep `stopWatchdogRef` → `sendHost.ts:90-95,100-105,293`, `sendStop.ts:21,49-96`, `useHostEffects.ts:30,83-89,112-116`, `HostRoot.tsx:192`. |
| 7 | History load + guard | **REPRODUCED** — `useHistoryHost.ts:143-249`: bump `:144`, key `:146-152`, `beginHistoryLoad` `:172`, epoch re-checks `:166,199`, `settleHistoryLoad` `:196`, three alerts `:206,217,227` (+ throw-guard `:179`), lossy flush `:235-244`, readable-first `:190-195`. | Partly. `src/engine/sessionPersistence.test.ts:224` boot-raw shape exists (shared, untouched). **The named new test (corrupt raw → quarantine written, refusal counts, flush after settle) does not exist** against the host load path. |
| 8 | Four save FIFOs | **REPRODUCED (3 of 4 + engine untouched)** — conversation index `.then(run, run)` `useConversationHost.ts:79`; library `.then(run, run)` `libraryHost.ts:97`; turn-end `settleHold` + 10 s fallback `sendFinalize.ts:131-147` (`HISTORY_WRITE_FALLBACK_MS` `:39`, fallback timer `:138`); engine FIFO untouched (`LlamaService.ts:1033`). `turnEndSavePromiseRef`/`sendInFlightPromiseRef` not lifted — reported at `sendFinalize.ts:14-17`, and their only old reader was the background discard (`Chat:2112-2115`). | Test **no** (first-save-rejects → second runs: absent in `src/host/**`). Grep `.then(run, run)` → **5 repo-wide**: `src/host/useConversationHost.ts:79`, `src/host/libraryHost.ts:97`, `src/app/AppShell.tsx:1066,1248`, `src/notes/NotesStore.ts:189`. |
| 9 | Epoch-stamped in-flight turn persistence | **REPRODUCED** — 400 ms debounce `useHistoryFlushes.ts:53-63` (stamp `:57`), 10 s throttle `:65-84` (stamp `:83`), AppState flush `:87-131` (stamp `:91`, landing-keyed `saveEngineSession` `:117-127`), unmount flush-then-abort `useHostEffects.ts:104-118`, turn-end `sendFinalize.ts:124-160`. Epoch is the writer's (`opts.epoch`, `historyWrite.ts:76-86`) instead of the old `getEpoch` closure. | Named tests (bump-in-window → no write; unmount mid-stream → exactly one `setItem`) **do not exist as such**; the drop logic is covered indirectly by `historyWrite.test.ts:78-105`. Grep `getEpoch()` in `src/host/**` → only the `getEpoch: () => history.writer.epoch()` shims (`HostRoot.tsx:166`, `sendHost` params). |
| 10 | Engine lifecycle | **REPRODUCED for load+boot; NOT REPRODUCED for the background machine.** Load: `src/host/engineEnsure.ts:38-271` (thermal preamble `:44-59`, gen capture `:61-66`), `src/host/engineEnsureLoad.ts:56-281`, gate/fallback `src/host/engineLoad.ts:104-231`; boot selection `src/host/usePipelineScans.ts:94-140` (saved id read `:100`, `pickStartModel` `:104`), unmount dispose `:141-166`; thermal hook `HostRoot.tsx:60-63`. **Not lifted:** the background/foreground discard state machine (old `App:3026-3526`, 501 lines — `grep BackgroundDiscard\|awaitLifecycle` in `src/host/**` = 0) and the thermal hard-gate **edge** effect (old `App:3672-3748` — `grep thermalGateEdge` = 0). | Replaced. Grep `ensureEngineForModelRef.current =` → **2 assignments**: `src/host/useModelHost.ts:202` (new) + `src/app/AppShell.tsx:4446` (old), plus doc comment `src/host/engineEnsure.ts:7`. Named test (fit=unknown → allow + banner; foreground never calls ensure) **does not exist** — the foreground path it guards is unmounted. |
| 11 | `handleSendStream` (1357 lines) + one bridge | **REPRODUCED**, split at the block's own seams: `src/host/engineTurn.ts:53-331` (orchestration, gate order in header `:13-24`), `engineTurnWindow.ts:38`, `engineTurnCompactor.ts:47`, `engineTurnSlide.ts:39`, `engineTurnStream.ts:38` (single `streamAssistantTurn` + bridge), `engineTurnMemory.ts:41` (extract + `afterSessionSave`). | **Yes.** `src/app/engineCallbackBridge.test.ts` still exists against the untouched bridge. Grep **`bridgeEngineCallbacks(` = 2 call sites** (changed meaning, as anticipated): `src/app/AppShell.tsx:6630` and `src/host/engineTurnStream.ts:238` — plus definition `src/app/engineCallbackBridge.ts:41` and its test usage `engineCallbackBridge.test.ts:31` (4 textual matches total). Bridge called once per app ✓. |
| 12 | Model index storage | **REPRODUCED** — key `src/host/modelSwitch.ts:43`, write `:121`, boot read `src/host/usePipelineScans.ts:100`, `modelIndexRef` `src/host/useModelHost.ts:71-73`, hash re-save only when ready + not sending `modelSwitch.ts:149-161`, boot never auto-flips (pick logic `usePipelineScans.ts:104-135`). | Replaced, meaning changed. Grep `MODEL_STORAGE_KEY` → **4 files / 8 matches**: `src/app/AppShell.tsx:405,2948,4501`; `src/host/modelSwitch.ts:43,121`; `src/host/usePipelineScans.ts:40,100` (import + read); `src/host/useModelHost.ts:37` (import). Per-app sites remain exactly 1 read + 1 write. |
| 13 | Conversation index store | **REPRODUCED** — state/FIFO/bind/boot `useConversationHost.ts:67-144` (boot hash after index load `:120-121`); switch ordering `conversationActions.ts:82-117` (flush `:89`, UI-first `:95`, save old stem `:96-106`, bind `:107`, restore `:111`); delete `:177-219` (index first `:189`, quarantine `:193`, invalidate `:211`, epoch bump `:213`, restore `:214-217`); touched `:232-246`. | Partly. **The named test (switch with a pending flush writes the old chat's key) does not exist**; delete-order tests do (`src/host/conversations.test.ts:84-100` index-before-key; `historyWrite.test.ts:107-141` bump orders). Grep `bumpPersistEpochRef.current?.()` (old, 1 site `App:2506`) → new: injected param `HostRoot.tsx:143`, called **once**, `conversationActions.ts:213`. |
| 14 | Tool-flag persistence + refs | **REPRODUCED** — state+ref mirror `src/host/toolFlags.ts:31-53`, refresh `:55-73`, refs read mid-run by the executor `agentTurnOptions.ts:209,220,227`, threaded via `hostDeps.ts:97-124`. Differences reported in-code: no `toggleWebTools` (no switch), skip-ref → factory. | Replaced + tested. Old grep `staticPrefixNotifySkipRef.current = false` (3 matches `App:2357-2360`) → new mechanism `src/host/staticPrefixNotify.ts:12-20` (`skipNext` `:15`), **tested** `src/host/staticPrefixNotify.test.ts:4-15`, wired `useHostEffects.ts:65-73`. |
| 15 | Voice state | **NOT REPRODUCED** (beyond presence scan + TTS toggle). | The old check cannot exist by design: grep `voiceRunIdRef` → **old file only** (`Chat:950,1340,1367-1415,1536`); 0 in `src/host/**`. Partial coverage: `usePipelineScans.ts:176-190`, `HostOverlays.tsx:190-191`. |
| 16 | Share-in | **NOT REPRODUCED** — no `Linking` listener, no `shareIntent` import in `App.tsx`/`src/host/**`. | Nothing to run against: the named test (same URL twice → one prefill) has no target; grep `shareIntent` in the new tree = 0. |
| 17 | Notices — not a queue | **REPRODUCED** — one state slot + one 4 s timer `useNotice.ts:19-24`; render `HostNotice.tsx:16-36`. | Grep `setNotice(` → new: **2, both inside `useNotice.ts:20,22`** (its own slot); old: `App:3548,3550`. No queue anywhere — the check's rule holds. |
| 18 | Memory banner (set-only) | **NOT REPRODUCED — by decision, reported** (`memoryHost.ts:6-8`, `engineEnsure.ts:14-18`). PARITY already carries it as an owner decision. | Old grep still true: `memoryBannerKey` → declaration `AppShell.tsx:3021` only (+ prose comments `memoryHost.ts:7`); nothing to run against new code. |
| 19 | Compactor/digest per-chat maps | **REPRODUCED** — six Maps `src/host/turnCorpus.ts:23-45`, `MAX_DIGEST_CORPUS_MESSAGES` `:45`, hygiene/reset/sync `:106-186`, reset on delete `conversationActions.ts:183`, per-chat KV load `engineTurnCompactor.ts:47+`. | Grep `resetCompactorChat(` → **8 repo-wide, 3 call sites per app**: new def `turnCorpus.ts:179` + calls `engineTurn.ts:275`, `engineTurnCompactor.ts:112`, `conversationActions.ts:183`; old def `AppShell.tsx:769` + calls `:2476,5776,6048`. Topology matches. |
| 20 | Notes-context injection | **REPRODUCED (code), trigger missing** — cap `src/host/notesContext.ts:5` (24 000), injection inside the engine half `engineTurn.ts:246-257` (same point as old `App:5749`). But **no UI ever sets `options.notes`** (`sendHost.ts:244` sets research only) → the branch is dead until D1 row 14 lands. | Grep `loadNotesContext(` → **4 repo-wide, 2 per app**: `AppShell.tsx:416,5749`; `notesContext.ts:10`, `engineTurn.ts:248`. The grep check passes; reachability does not. |
| 21 | Static-prefix skip-once | **REPRODUCED + TESTED** — `staticPrefixNotify.ts:12-20`, wired `useHostEffects.ts:65-73` (deps include the three flags), test `staticPrefixNotify.test.ts:4-15`. | Yes — the named test exists against the new factory (old "test on the skip ref" is subsumed). |

### Greps whose meaning changed (verified counts)

| Grep (old meaning) | New reality |
|---|---|
| `bridgeEngineCallbacks(` = 1 | **2 call sites**: `src/app/AppShell.tsx:6630`, `src/host/engineTurnStream.ts:238` (+ def `engineCallbackBridge.ts:41`, test `engineCallbackBridge.test.ts:31`) |
| `ensureEngineForModelRef.current =` = 1 | **2 assignments**: `src/app/AppShell.tsx:4446`, `src/host/useModelHost.ts:202` (+ comment `engineEnsure.ts:7`) |
| `MODEL_STORAGE_KEY` = 2 sites | **8 matches / 4 files**: `AppShell.tsx:405,2948,4501`; `modelSwitch.ts:43,121`; `usePipelineScans.ts:40,100`; `useModelHost.ts:37` (per-app semantic pair: 1 read + 1 write) |
| `getEpoch() !== opts.epoch` (`Chat:619,629`) | replaced by `epoch !== stamped` — **2, both `src/host/historyWrite.ts:80,86`** |
| `updateMessage(` / `regenGenerationRef.current +=` / `++sendRunIdRef` | **0 in `src/host/**`** — replaced by fence: 27 `fence.*` call sites; `beginRun` 1 (`sendHost.ts:131`), `invalidate` 2 (`useHostEffects.ts:81,111`), `retire` 1 (`sendStop.ts:62`) |
| `abortRef.current?.abort()` = 7 | **2 in `src/host/**`** (`useHostEffects.ts:78,109`) + `sendStop.ts:46` direct `controller.abort()` |
| `.then(run, run)` | **5 repo-wide**: `useConversationHost.ts:79`, `libraryHost.ts:97`, `AppShell.tsx:1066,1248`, `NotesStore.ts:189` |
| `setNotice(` | new app: **2, `useNotice.ts:20,22`**; old `AppShell.tsx:3548,3550` |
| `bumpPersistEpochRef.current?.()` = 1 | injected fn: passed `HostRoot.tsx:143`, called **once** `conversationActions.ts:213` |
| `resetCompactorChat(` | **8 repo-wide** (3 calls per app — see D2 row 19) |
| `loadNotesContext(` | **4 repo-wide** (def+call per app — see D2 row 20) |
| `staticPrefixNotifySkipRef.current = false` | old-only (`App:2360`); new `staticPrefixNotify.ts:15 skipNext` |
| `voiceRunIdRef`, `memoryBannerKey`, `keyboardShouldPersistTaps` (`Chat:4025,4146`) | old-only — features/unmount not present in the new root |
| `shouldShowLongChatNudge` | old-only (`Chat:155,1321`) — see Section 4 |

---

## Table 3 — Gaps, ranked by what a user notices first

| # | Gap | What is lost | Old location | Size to restore | In PARITY already? |
|---|---|---|---|---|---|
| 1 | **First open is a blank band** | Photo, hour greeting, welcome line, 4 suggestion cards that send — a brand-new install or empty chat shows nothing but chrome | `Chat:191-194, 4015-4131, 423-453` | ~200-line block + `buildSuggestions` (~30) + raster require | **Carried** — D1 row 12 (✗) |
| 2 | **No streaming caret; stopped/failed turns look finished** | The answer grows with no caret; `interrupted` and error states render identically to a completed answer (state is set correctly — only the renderer is missing) | caret `Chat:5484,5559-5576`; interrupted row `Chat:5627-5630` | ~30 lines (caret in `Answer`/mapper field) + ~10 (marker row); mapper field in `messageMapper.ts:69-77` | **Carried** — D1 rows 9 (✗), 10/25 (◐) |
| 3 | **Export/share chat gone from the header** | Sharing a conversation off-device | `Chat:3206-3218`, button `Chat:4757-4769` | ~15 lines + Share API call in `Shell`/host | **Carried** — D1 row 2 (noted ✗) |
| 4 | **Every message interaction gone** | Long-press menu (copy/flash, save-to-notes, translate, edit, regenerate, cancel), inline chips (copy, read-aloud, more), edit modal, translate block, read-aloud, save-to-notes, regenerate | `Chat:4385-4496, 4500-4581, 3481-3599, 3533-3582, 1613-1660, 5419-5434, 5593-5624`; `App:3749-3763` | ~500–600 lines across modal + handlers + TTS glue (`ChatMessageRow` press paths) | **Carried** — D1 rows 15-21 (all ✗) |
| 5 | **Attach and mic are honest dead buttons; the whole attachment and voice pipelines are absent** | Attach sheet, image/PDF/docx import, context chips, whisper dictation, voice status/toast, voice cleanup — today both buttons answer with a toast (`shell.notice.attach` `HostRoot.tsx:256`, `shell.notice.mic` `:257`) | `Chat:1661-1833, 4222-4247, 1515-1611, 4299-4314, 1412-1426`; `App:5155-5320` | attachments ~173 + sheet glue; voice ~307 + download block ~158 | **Carried** — D1 rows 7, 29-32, 43 (stubs carry their toasts per §2.7) |
| 6 | **Model bar lost its status life** | Download %, thin progress bar, error+hint lines under the strip, battery ETA line — the strip is name+where only | `App:6971-7008, 6746-6788, 2625-2712, 2758-2830` | model-bar block ~130 + notify ~88 + battery builder ~40 | **Carried** — D1 rows 33-36 (◐/✗); row 35's hint builder lives in Settings only |
| 7 | **No downloads at all** | Model / Whisper / embedding downloads, progress, notifications, confirm dialog — Settings buttons toast `shell.notice.download` / `voiceDownload` / `embeddingDownload` (`HostOverlays.tsx:178,190,199`) | `App:4657-5154` (442), `App:5155-5312` (158) | named module: the download pipeline (~600) | **Carried** — D1 rows 31, 34 (✗) |
| 8 | **Share-in dead** | Android `url` intent prefill, .txt/.md/PDF import, busy/too-large/failed notices | `App:3553-3671`, `shareIntent.ts:37` | ~119 lines | **Carried** — D1 row 41 (✗) |
| 9 | **Chips and sheets missing**: research/notes/doc chips, quick templates | One-tap deep research, notes mode (engine branch exists but unreachable, `engineTurn.ts:246`), template → draft flow | `Chat:4189-4221, 4375-4380, 3633-3653` | chips ~50 + sheet call ~20 (component exists) | **Carried** — D1 rows 13, 14 (✗) |
| 10 | **CTA buttons never render** though capture+persist work — a restored answer carries ctas nobody can press | Visible action buttons on answers | `Chat:5764-5790` | renderer ~27 + mapper field | **Carried** — D1 row 26 (✗) |
| 11 | **Mini-app card + sheet unreachable** | Interactive mini-apps render nowhere; overlay kind deleted (`hostOverlay.ts:7-16`); engine still produces them (persist `historyMessages.ts:120-131`) | sheet `App:7214-7252` (~38), card `Chat:6033-6093` (~60) + mapper field | ~100 + sheet chrome re-expression | **Carried** — D1 rows 4, 28 (✗) |
| 12 | **PDF library import and document_chat's PDF path break in the new shell** | `PdfTextExtractorHost` is mounted only in the old branch (`App:7257`); `requestPdfText` rejects immediately with no host (`src/pdf/pdfTextService.ts:113-115`), so `DocumentsScreen.tsx:386` and `src/host/agentTurnOptions.ts:111,139-140` cannot extract PDF text | `App:7257` | one mount line in `HostRoot` (~2 lines) | **Not carried as a feature row** — only named in D3c as a service "the two files merely call" (PARITY:230). **Adding it is warranted.** |
| 13 | **Persona row leaves the keyboard up**; transcript taps aren't `keyboardShouldPersistTaps="handled"` | Keyboard dismissal on persona press; chip taps while the keyboard is open may swallow the first tap | `App:7104`; `Chat:4025,4146` | 1 line + 1 prop | Row 1/45 partially (the persona-dismiss site is in row 1's citation but not called out; `keyboardShouldPersistTaps` is carried in row 45 with drifted line numbers 4029-4030 vs actual 4025/4146) |
| 14 | **kb-debug badge gone** (dev-only) | The keyboard-debug overlay behind `kalsa.kbDebug` | `Chat:865-904, 4668-4691` | ~40 lines | **Carried** — D1 row 50 (✗, keep/drop pending) |

---

## Section 4 — behaviour no PARITY row covers (so the new code probably dropped it)

1. **The painted background is gone.** Old root renders `<PainterlyBg />` (`src/app/AppShell.tsx:6849`, component `src/theme/components/PainterlyBg.tsx:10`); `grep PainterlyBg` over `src/host/**`, `src/ui/shell/**`, `App.tsx` = 0. It appears in PARITY only inside D3a's JSX row (`docs/PARITY.md:160`) — **no D1 row demands it**. A user sees a flat `shell` colour instead of the app's background art.

2. **`PdfTextExtractorHost` is never mounted in the new root** (old `App:7257`). Consequences above (Table 3, gap 12): with no host registered, `requestPdfText` rejects on call (`src/pdf/pdfTextService.ts:113-115`), so **adding a PDF to the Documents library from the new shell fails**, and the document tool's PDF page path fails (`src/host/agentTurnOptions.ts:111,139-140`). PARITY's D3c line (`docs/PARITY.md:230`) records the mount as a fact but no feature row holds the rewrite to it. *How it surfaces depends on `DocumentsScreen.tsx:386`'s error handling — that file decides whether the user sees an alert or a silent stall.*

3. **The old strip's "New chat" and the drawer's "New chat" were different behaviors; the new strip runs only the drawer's.** Old nav new-chat called `clearChat` (`Chat:3949`, `Chat:4004` → `clearChat` `Chat:3219-3328`: synchronous abort → flush → epoch bump → wipe messages/draft/attachments/modes/voice/translate/menu, then `onNewConversation()` at `Chat:3318-3322`). New strip + drawer both call `handleNewConversation` (`HostRoot.tsx:255,280` → `conversationActions.ts:119-172`), which flushes and switches but performs **no synchronous reset of the active thread** — the reset that exists (draft/tools, `HostRoot.tsx:117-120`) runs later via the load effect, and the abort runs after commit (`useHostEffects.ts:78`). PARITY D1 row 2 records "new chat" as present without this semantic difference, and D2 row 1's rule "clear flushes before bump (Chat:3228-3240)" now has **no production caller**: `flushThenBump` (`src/host/historyWrite.ts:95-103`) is exercised only by `historyWrite.test.ts:107`.

4. **The 180-second foreground idle dispose never runs.** Old app assigns `bumpForegroundIdleRef.current` inside the background/foreground effect (`App:3311`) and bumps it at turn start and download start (`App:4676, 5417`), consulting `shouldRunForegroundIdleDispose` (`App:3284`; constant `FOREGROUND_IDLE_DISPOSE_MS = 180_000` at `src/app/foregroundIdleDispose.ts:4`). The new host calls the ref but **nothing ever assigns it** — stated at `src/host/hostDeps.ts:132-137` ("the (unmounted) idle-dispose system is the only thing that would ever assign it"), call `src/host/engineTurn.ts:133`. PARITY never names foreground idle dispose (grep over `docs/PARITY.md` = 0 hits); D2 row 10 carries only the background/foreground *discard machine* as a unit. Consequence: in the new shell the engine stays resident after idle — invisible in UI, visible on the battery/thermal campaign the project rules care about.

5. **The long-chat nudge is gone.** Old one-shot nudge per conversation: state `Chat:921-922`, effect `Chat:1314-1327` over `shouldShowLongChatNudge` (`src/chat/longChatEstimate.ts:132`), rendered in the old JSX ("nav, **nudge**, empty state…", D3b `docs/PARITY.md:200`). New tree: `grep shouldShowLongChatNudge` → old file only; `src/host/useHistoryHost.ts:13` admits dropping only the *reset* (`setLongChatNudgeShown`), not the nudge. No D1 row mentions it; D3b names the unit at `docs/PARITY.md:184`. Users lose the heads-up that a long conversation starts dropping context.

6. **`/bench …` / `bench:…` debug commands no longer intercept.** Old `handleSend` branched on `isBenchCommand` and answered `tryHandleBenchCommand` (`Chat:2317-2330`; `src/bench/benchConfig.ts:824,848`). New send has no branch (`grep isBenchCommand` in `src/host/**` = 0) — such text goes to the model as prose. PARITY has **zero** "bench" rows; `sendHost.ts:8-15` reports it in code only. Harness impact could not be determined from this pass: the scripts that type into the composer (`scripts/ci-e2e.sh:37`, `scripts/ci-dflash-ab.sh:185`) send ordinary prompts and seed bench config via storage (`scripts/ci-e2e.sh:77`), and no `bench:`-typing was found in `scripts/**` — but a full harness audit would decide it.

7. **Ordering nit worth a line in the doc (D2 rows 5/13):** on conversation switch/delete the new code flushes **before** it aborts the live send (`conversationActions.ts:89` flush → commit → `useHostEffects.ts:78` abort), where old `clearChat` aborted first (`Chat:3231`). Fence ownership (`useHostEffects.ts:81`) makes this safe for state, but the flush now deliberately persists a still-streaming snapshot (`allowStreamingPartial`) that the old abort-first order raced differently. Carried implicitly by D2 rows 5/13; not spelled out.

---

## Could not determine (and which file decides it)

| # | Question | Deciding file |
|---|---|---|
| 1 | Whether the e2e/campaign harness ever types `/bench` or `bench:` into the composer (would make Section 4.6 harness-critical) | `scripts/ci-e2e.sh`, `scripts/ci-dflash-ab.sh`, any `scripts/campaign/*` sender |
| 2 | How a failed PDF import surfaces in the new shell (alert vs silent stall) once `PdfTextExtractorHost` is absent | `src/screens/DocumentsScreen.tsx:386` + its catch path |
| 3 | Whether the owner wants `clearChat`'s flush→bump→synchronous-reset order re-expressed as a production caller of `historyWrite.flushThenBump` (`historyWrite.ts:95`), or accepted as flush + load-effect bump | `src/host/conversationActions.ts:119-172` + `src/host/useHistoryHost.ts:144` |
| 4 | Whether idle-dispose (Section 4.4) is scheduled with the background-machine lift or dropped by decision | `src/app/AppShell.tsx:3026-3526` + `src/app/foregroundIdleDispose.ts` |
| 5 | Test results — no jest was executed in this walk (read-only pass; a capture build was running); all test claims are existence-only | run `npx jest src/host src/ui/shell` when the machine is free |
| 6 | Minor: PARITY's `keyboardShouldPersistTaps` line refs (`Chat:4029-4030`) are drifted; actual lines `Chat:4025,4146` | `src/screens/AiChatPage.tsx:4025,4146` |
