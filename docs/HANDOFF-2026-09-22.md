# HANDOFF — 2026-09-22

Written so the next agent starts from state, not from a summary of a summary. Every claim is a
commit id, a file path, or a command's output.

## 1. The goal, in one line

Each device — the PC and every paired phone — gets its own conversation and its own cache namespace
on one engine, and a chat's KV cache survives being put on disk and comes back warm, while the app
never shows a number it cannot say where it came from.

## 2. State of the repos

**App — `/Users/marco/Projects/kalsa-brain`, branch `brain`, HEAD `5ef2388`.** `origin/brain` is the
previous session's `cac9068`; **nothing pushed this session by the orchestrator or its agents** (the
push of the earlier commits was the owner's). Working tree clean; suites at rest: workspace
**857 passed / 0 failed / 20 ignored**, supervisor **68/68**, src-tauri **167/167**, `npx tsc
--noEmit` 0, `node dev/smoke-react.mjs` *"ok: 51 states rendered, copy rules hold"*, `node
chat/scripts/tier-panel.mjs` all green, `python3 dev/test-release-provenance.py` exit 0,
`python3 dev/test-engine-pin.py` green.

Written this session, oldest first:

```
fe14e78  measure: the concurrency artifact carries its provenance as fields, not as prose
1f22bb7  app: the panel's concurrency row, labelled with the release it was measured on
35458f1  app: the row's value is the number; the source line carries the reading
36c96fc  docs(the plan): paid provenance, the released T1, and the engine the app installs
6efec90  state: a stop in flight is `Stopping`, and it suppresses the door's re-raise
34de14f  measure: a manifest with no usable digest is not a fork build
5ffcb11  app+state: the Models page shares the state type, the square is pinned, the wedge declared
f5b57a3  docs(the plan): the third run, the released T1, the drain that is now a state
17e016b  tier-panel: the constant must match its artifact exactly, not at the rounding it shows
f1df191  presence: `Stopped` needs proof — a reaped child, a pid and a port each say what they know
ae78761  suspect: the record a stop could not disprove, recovered or replaced by the next start
f9d8861  state: the dead worker's drain is answered, not left `Stopping` forever
f7ac758  presence: the probe is an argument, so the policy tests stop racing a real socket
e3f47a7  engine: the app installs kalsa-server-v1.1.1, because the tier's warmth is T1
d540934  engine pin: the check states the release itself, so a wrong `home` is not chased
5ef2388  tests: the stand-in stops inventing an action from a failed read
```

**Engine — `/Users/marco/Projects/kalsallama`, branch `main`, HEAD `833cde99b` (T1).**
`kalsa-server-v1.1.1` exists (commit `a7d2cec79`, tag object `0f1b3bec…`); T1 is an ancestor of it
and NOT of `kalsa-server-v1.1.0` (`2a290390d`) — verified read-only with
`git merge-base --is-ancestor 833cde99b <commit>`. Nothing was written to that repo or to
`llama.rn-kalsa`.

## 3. What this session delivered

**The panel's concurrency row, and the provenance under it.** The row reads `Two devices decoding` /
`0.73x each, 1.47x together` / `Decode rate vs one device, not wall time — measured on
kalsa-server-v1.1.1, macos-arm64/metal`. The number cannot age silently: it is one constant beside
the identity of the release it was measured on, and `chat/scripts/tier-panel.mjs` compares the two
**exactly**, at the four decimals the constant carries, with the two decimals the row displays as a
separate check.

**The provenance is derived, never asserted.** `platform`, `backend` and the whole release identity
are matched by the EXECUTED binary's sha256 against `artifacts[].exe_sha256` of the published
manifest (`https://dl.kalsa.io/kalsa-server/v1.1.1/manifest.json`, whose own sha256 and fetch
instant are fields too). Three states, never conflated: `matched`; `not-the-release`, carrying §9's
label `fork build, not the release`; `unverified`, which a network failure produces and
`not-the-release` never is. Each carries a machine-readable `reason_code`.

**The engine the app installs** (`e3f47a7` + `d540934`, review FIT). The row pinned
`kalsa-server-v1.1.0`, which does not contain T1 — so the warmth was released and not delivered. It
now pins v1.1.1, and the delivery rests on two facts that were verified rather than assumed: the
archive **set** in `marker.rs` is what tells versions apart (so a machine that already has v1.1.0
re-acquires; the identical `exe_sha256` cannot vouch for a version), and the inlet's probe reads the
**dylib**, not the launcher, so "the launcher is identical" does not cover it — the dylib's bytes
were checked directly (`x-kalsa-slot` once, `X-kalsa-Slot` never). `dev/test-engine-pin.py` states
the release **itself** and compares the row to the published manifest field by field; the first
version took its anchor from the row's own `home`, which meant a wrong `home` was chased instead of
caught, and only a mutation on that silent field found it.

**`Stopping`** (`6efec90`, corrections `5ffcb11`). A stop in flight is a state, not a flag, because
the outcome used to depend on two actors writing the same field. The caller declares the drain
BEFORE the command is queued — that ordering IS the window, closed from its first instant — and
`crates/kalsa-supervisor/src/drain.rs` enforces that while the state reads `Stopping` the only
writes that land are the drain's own ends. The `brain_state` arm lowers the door and the pairing
square and never raises them.

**The `Stopped` invariant** (`f1df191`, `ae78761`, `f9d8861`, `f7ac758`). `Stopped` needs both halves
and the witnesses are not interchangeable (`presence.rs::settle`): a **reaped child** is the kernel's
proof about OUR process, so the port is **not a veto** — if something answers it afterwards,
`Stopped` is still true of our engine and what answers is a suspicion **recorded**; a **known pid**
alive after both signals is a survivor whatever the port says; a known pid now dead is corroborated
by the port (§9's "pid AND port"); an **adopted-blind** engine gets `Stopped` only after the probe
FAILS, plus the record, because the port is then the only witness there is. Anything else closes the
drain as `StopUnconfirmed` with its measures. The suspicion lives beside the state file
(`<state file>.orphan`, `suspect.rs`) and the next start recovers it (port silent) or replaces it
(adoption by port and health). `child::Termination` says whether the walk needed a grace, needed
SIGKILL, or never saw the process leave; a failing `kill` is a datum.

## 4. The measurement, and what it is a measurement OF

`dev/results/concurrency-two-devices/` — three runs of one question, three load floors; the panel
carries the third (`34de14f`):

| run | commit | per device | aggregate | A2/A | floor (1m) |
|---|---|---|---|---|---|
| 1 | `5b9b98f` | 0.7546 | 1.5093 | 1.0105 | ~4 |
| 2 | `fe14e78` | 0.7302 | 1.4604 | 0.9982 | ~2 |
| 3 | `34de14f` | **0.7336** | **1.4672** | 1.0037 | 4.08 |

Two devices do not halve and do not serialise. The spread *between* runs (3 % in the per-device
term) is the honest part; *within* each run the `A2/A` bracket is inside ±1 %, so the machine did not
move under any of them. Every number is a field of the artifact — argv, model sha256, `nice`,
`--max-load`, `attempts_max`, loadavg before and after, the release identity. The floor is declared,
not denied: the machine was never empty (this harness), which moves the absolute rate and cancels in
the comparison.

**The warmth proof from earlier tonight ran a fork build, not the release** —
`dev/results/unload-restore/` used `/Users/marco/Projects/kalsallama/build/bin/llama-server` (build
11193, commit `833cde99b`). Both carry T1 by ancestry; only the release carries a published identity.

## 5. Where the bodies are buried

- **A manifest that was READ but published no usable digest was accused of being a fork build.**
  Missing, empty, `null`, an integer and uppercase hex all produced the strong
  `fork build, not the release` label. Weak evidence was read as a verdict — the one class the
  original mutation could not catch, because that mutation tested a match that was too WIDE.
- **A check that compares at the precision it DISPLAYS is blind below it.** The panel check used
  `toFixed(2)` on both sides, so any drift under 0.005 on any of the three ratios stayed green; the
  historical red arrived only because the aggregate happened to move 0.0068.
- **A DTO declared twice drifts.** `ModelsSurface.tsx` had its own `kind` union without `"stopping"`,
  so the real DTO fell into a blind `default` and told the owner it could not tell what the computer
  was running — for the whole teardown, while the state said exactly what was happening. Fixed by
  importing the shared type and making the switch exhaustive with a `never` arm: a new kind is now a
  COMPILE error, not a false sentence.
- **A presence probe is not a readiness probe.** `health_ok` answers `false` for ANY failure —
  refused, timeout, 503 "loading", garbage — so a live but slow engine reads as absent.
  `presence.rs` exists for the distinction: NOTHING LISTENING (connection REFUSED, positive evidence
  of absence) versus SOMETHING LISTENING (connected, then answered 503, answered 200, or said
  nothing — presence either way).
- **The witnesses are not interchangeable, and that is the invariant.** A naive "pid dead AND port
  silent" would have made three existing tests red (`tests/supervisor.rs:38,106,122` — the child is
  reaped while the fixture's own `FakeHealth` still holds the port), and the cheap way out would have
  been to change the fixtures. The right answer was to make the code tell the truth: a reap is a
  proof about OUR process, the port is a separate suspicion.
- **Escalation is not failure.** A child that needs SIGKILL after ignoring SIGTERM ends CORRECTLY
  (`stop_escalates_to_sigkill_for_a_wedged_child`, `fake_stubborn.sh`). What decides `Stopped` is the
  presence, never that a grace expired — and the expiry is logged, not punished.
- **`let _ = wait_pid_gone(pid, grace)`** (`child.rs`) swallowed the second wait: a survivor after
  SIGKILL returned `Ok(())`, and `signal(pid, Kill)` ignored the `kill` error (EPERM/ESRCH). That
  `let _ =` is what §9 calls "the grace expiry recorded instead of swallowed".
- **The drain guard and the failed-to-stop end.** `drain::set` refuses every write but the drain's
  own ends while draining. A failed-to-stop is one of those ends and had to be admitted explicitly —
  otherwise the guard makes a survivor invisible, which is the lie the invariant prevents.
- **A test that binds, drops a listener and then demands REFUSED from that port is a test whose
  fixture lies.** At rest the kernel refuses every time (2000 of 2000); under socket churn the window
  opens, the connect lands on a dying or reassigned state, the probe answers `There{Silent}`, and the
  test sees a ghost. It cost 5 failures in 16 runs and a one-in-twenty-one mystery whose name a pipe
  ate. Closed by making the probe an **argument** (`presence::Probe`): policy tests inject
  `Gone`/`There`/`Unknown`, the refusal's classification is tested as a pure function of an
  `io::Error`, and only the deterministic socket cases keep a real listener.
- **A fixture must never turn a read failure into a PLAUSIBLE value.** The same one-in-twenty-one
  mystery had a second cause, found by measurement after an analysis had pointed elsewhere: the test
  stand-in's listener is non-blocking and, on macOS, an accepted socket **inherits `O_NONBLOCK`**, so
  its first read of the door's request returns `WouldBlock` — in 4.75 µs, not after the 5 s `SO_RCVTIMEO`
  that is inert on a non-blocking socket. `unwrap_or_default() == 0` read that as an empty head, the
  head parser fell to `.unwrap_or("none")`, the stand-in **invented the action "none"**, answered
  200 anyway, and the door's own restore was recorded as a success that never happened — only the
  fixture's assertion failed. The fix is `set_nonblocking(false)` after the accept (as the twin
  fixture and production already do) plus a head that is incomplete or has no `action=` **being
  dropped and reported** instead of answered: an incomplete head must never become an action. With a
  forced window the failure mode is now a loud 502. **A check that invents a plausible value from a
  failure is the most expensive kind of green**, and it can hide in a fixture as easily as in a test.
  Two facts about the platform were measured, not assumed (a from-scratch probe: `Err(WouldBlock)`
  after 1.9 µs with no timeout, after 302 ms with a 300 ms timeout on a normalized socket): the
  accepted socket inherits `O_NONBLOCK`, and `SO_RCVTIMEO` is **inert** on it — so a timeout set on
  an inherited socket never fires, which is why the first analysis's "stalled for 5 s" trigger was
  structurally unreachable. **And the nuance nobody had written**: expiring that timeout surfaces as
  kind `WouldBlock` too, the same kind as "nothing is there yet" — the discriminator is the **time**,
  not the kind, and a reader who trusts the kind alone is deceived twice. The comment in the code says
  "(macOS)" and that scope is correct: Linux's `accept4` does not inherit it, and nobody has measured
  Linux here. One last place in the repo still reads on an inherited non-blocking socket without
  normalizing — the supervisor tests' `FakeHealth` (`tests/common/mod.rs:54`) — but it discards what
  it reads and parses nothing, so it cannot fabricate a value; declared, and left alone.
- **A mutation script whose anchor does not match fails silently**, the assertion aborts, and the
  suite runs the UNMUTATED source: a vacuous green produced in the same session that was attacking
  vacuous greens. Make every mutation script assert it changed the file.
- **`$?` after a pipe is the pipe's exit, not the test's.** `npx tsc --noEmit | head` reported 0 for
  a run that exited 2. And `awk`-summing a suite's output ate the NAME of the one test that failed
  once: a sum is not a record.
- **The measurement gate works.** During the third run `--max-load 6.0` refused twice (load 6.6 →
  31.9, while a reviewer compiled Rust); the run waited ~9 minutes rather than raise the ceiling.
- **APFS is case-insensitive**, the engine's xdist worker 5 wants port 8130 (the app's own engine),
  and a stale engine can satisfy a health check — all three from the previous handoff still stand.

## 6. Standing rules

Unchanged from `docs/HANDOFF-2026-09-21-b.md` §7 and `docs/PLAN-DISK-TIER.md` §7. The three that did
the most work tonight:

- **Name the repository, the branch and the HEAD**, always — five repositories and two worktrees
  share names and remotes.
- **A test or a pin must be able to go red for the reason it declares, and the proof is a mutation
  that kills it** — mutated by the reviewer, not by the author's own replay. The reviewers found more
  by mutating than by reading, and one of them found a false number in a commit message.
- **Stage by name.** Never `git add -A`, never `.`, and no push, tag or release without the owner's
  explicit OK.

## 7. Open questions for the owner

1. **The engine pin is delivered** (`e3f47a7` + `d540934`, review FIT): the app installs
   `kalsa-server-v1.1.1`, which carries T1, so the tier's warmth is no longer released-and-not-
   delivered, and the panel's row now cites the release the app actually installs. **The push is the
   owner's call**, not the orchestrator's: nothing on this branch was pushed by this session's
   agents.
2. **`inlet.rs`'s comment no longer says "the shipped v1.1.0 module"** (`44d6441`): it now carries the
   counts for **both** pinned releases together with the command that redoes them, because a comment
   about an artifact that is not in the repo has to travel with its method. Two more mentions look
   like the same trap and are **not**: `crates/kalsa-catalog/src/manifest.rs:830` and
   `crates/kalsa-launch/src/policy/slot_cache.rs:158` say "MEASURED on the v1.1.0 engine, THIS / the
   pinned file" - and that file is the pinned **MODEL**, in a catalog row and in a model's KV policy,
   not the engine, whose pin lives in another crate. They are true as written; they stay, and they are
   recorded here so nobody re-flags them as the pin's leftovers. The rule the next reader needs:
   **check what a pronoun refers to before declaring a sentence false - a flag is a hypothesis, not a
   finding.**
3. **A cosmetic leniency, declared not fixed**: a manifest digest padded with whitespace is accepted
   after `strip()` while a `0x`-prefixed one is not. Both paths can only push toward `unverified`,
   never toward the fork accusation, so it is safe by construction — and fixing it would change
   `measure-concurrency.py`, drift the artifact's `script_sha256`, and buy a fourth measurement for
   nothing.
4. **`--max-load 0` means no ceiling** (documented, recorded as `0.0` in the artifact): a gate cannot
   be turned off any other way, which is the point, but it is a choice.
5. **The identity of the socket acceptor in the churn experiment was never caught** — three live
   captures with `lsof`/`netstat` came back empty about 10 ms after the hit. The behaviour is
   understood and no longer testable-by-accident; the mechanism is not traced to a kernel state, and
   nobody should claim it is. **Superseded for the src-tauri flake**, which is closed with a measured
   mechanism (see §5); the acceptor question remains open for the *socket-churn* experiment alone.
6. **`dev/measure-slot-restore.py:45` still carries a `builds/metal/kalsa-server-v1.1.0/` path.** It
   is a measurement harness, not the delivery — the same class as the seven historical records — so it
   was left alone deliberately; whoever next runs it on the new engine should decide whether the
   harness should take the release as an argument instead of a constant.
