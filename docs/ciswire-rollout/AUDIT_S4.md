# AUDIT S4 — tool-help leg (uncommitted, 2026-08-25 session)

Scope: `src/rules/{toolGateRegistry,calendarGate,gateAuditLog,runToolGate,evaluate}.ts` (+tests), `webSearchTool.ts`, `AppShell.tsx` executeTool, `i18n/en|it.ts`. Spec: PLAN_CISWIRE_FOR_REAL.md Part 2 S4 (owner decisions: calendar structural-only, miniapps exempt, block+warn, registry chokepoint, inject deferred).

Verification runs (item 8, run by auditor): `npx tsc --noEmit` → **exit 0**. `npx jest src/rules/ src/memory/` → **7 suites / 27 tests pass, exit 0**.

## Findings

| # | Verdict | Finding | Evidence |
|---|---|---|---|
| 1 | **CONFIRMED-OK** | **SINGLE CHOKEPOINT holds.** `runToolGate` has exactly one production call site — `executeTool` (AppShell.tsx:1973). All side-effecting executors are reachable only inside executeTool after the gate: `searchWeb` (webSearchTool.ts:82), `fetchExec` (:1995), `runCalendarAgenda` (:2010), `documentExec` (:2016), device tools (:1996-2006). Deep research reuses executeTool (:4454-4465); auto-doc injection reuses it (LlamaService.ts:2824); round tools (:3122). Only other executor-adjacent path: miniappActions.ts — pure CSV/export/share, no tool dispatch (grep clean). Gate sits AFTER enablement refuses (:1943-1967) + `privateSearchLatchSeq` refuse (:1969-1971) — all side-effect-free — and BEFORE any executor. Engine skip paths (cap/dup/failed-repeat, LlamaService:3075-3118) precede executeTool but perform no side effects. **Streaming**: tool results are materialized strings (`role:"tool"` content, LlamaService:3134+); nothing streams partial tool output — a prepended warn note cannot corrupt a stream. `applyWarnToResult` spreads the whole result (`{...result, text}`) → `sources`/`kind`/`strategy`/`passages`/`error` survive; `isSuccessfulToolOutcome` keys on error/strategy only (turnTelemetry.ts:107-115); source accumulation uses the `.sources` array, cite-appendix appends after body (en.ts:703). | runToolGate.ts:98-133; AppShell.tsx:1934-2049 |
| 2 | **CONFIRMED-OK** (2 documented deltas) | **Flag-off byte-identity holds for the interface that matters.** web_search: SAME table object (`TOOL_GATE_TABLE`, toolGate.ts untouched since Aug 14), same thresholds 0.40/0.15/abstains, same refusal strings (`webSearchPrivacyBlocked`, `emptySearchQuery` via blockText runToolGate.ts:46-51). evaluate.ts diff is warn/ruleId additions only — block branch byte-identical to HEAD. Gate input: `lastUserMessage` = `lastUserRawRef.current` — the SAME value HEAD fed searchExec (HEAD AppShell:1952 vs now:1936-1939,:4212); `query` equivalently shaped (rule trims). Empty query: HEAD returned `emptySearchQuery` pre-gate; now empty-query rule (priority 30) returns the identical string. calendar: `resolveToolGateTable("calendar_agenda", false)` → undefined (toolGateRegistry.ts:27-29) → `{blocked:false}` (runToolGate.ts:98) → same dispatch. **UNGATED: verified.** No audit writes: `appendGateAudit` only inside `opts.toolhelpOn` (runToolGate.ts:114-121); unit-covered (runToolGate.test.ts:22-33). WarnNote only when flag on (:131-133). **Delta 1 (S3-owned, sealed, not S4):** echo-guard fact set widened from last-10 texts (HEAD `getMemoryFacts().slice(0,10)`) to `dna.keptTexts` (10-40) — S3 audit #3 covered this. **Delta 2 (pre-existing dead knob) → finding 9.** | runToolGate.ts:98,114; toolGateRegistry.ts:20-29 |
| 3a | **DEFECT** | **Calendar empty/malformed rules contradict the tool's own documented defaults.** `CALENDAR_AGENDA_TOOL` advertises "Default: today 00:00 local" / "Default: tomorrow" and does NOT mark fields required (calendarTool.ts:16-39); `resolveAgendaRange` defaults missing values, auto-corrects inverted ranges (to ≤ from → next day), clamps >14 d (calendarAgenda.ts:27-41). Flag-ON gate blocks the SAME call the tool is documented+built to serve: `calendar-empty-range` fires on EITHER field missing (calendarGate.ts:41-49), `calendar-malformed-range` on garbage OR inverted (:51-59). A model omitting toISO for "what's on today" (schema-permitted) → hard block. This is §1.1's failure mode verbatim (gate costs a working feature). Spec said "empty-range block" but the tool contract says empty is DEFAULTABLE — implementer chose the stricter reading. Also: inverted-range calls the tool self-heals are blocked. | calendarGate.ts:41-59; calendarTool.ts:16-39; calendarAgenda.ts:27-41 |
| 3b | **PLAUSIBLE-RISK** | **calendar-private-data overblock on the user's own message — empirically verified.** `stringFields` scans every string in the snapshot (calendarGate.ts:30-36,62-70) = effectively `lastUserMessage` (+ any model-hallucinated extra string args; fromISO/toISO are inert — ISO instants normalize to ONE opaque token, `"2026-08-25T00:00:00.000Z"` → `"20260825t000000000z"`, so date fields cannot collide with fact tokens; year-token false-positive hypothesis DISPROVEN by execution). But: `containsPrivateData("events in Rome this weekend", "The user's mother lives in Rome")` → **true** (Signal 3 consecutive pair `in rome`); `containsPrivateData("Rome events", …)` → **true** (Signal 2 ≥50%); IT mirror `"appuntamenti a Roma"` vs fact `"La mamma vive a Roma"` → true. Any memory fact naming a city/person/place makes legitimate agenda messages mentioning it permanently blocked (facts in the injected set for the whole turn set). Test asserts this is intended (calendarGate.test.ts:50-57). Spec-mandated (owner: "privacy containment") — but this is exactly the blunt-gate class §1.1 warns about; the containment primitive was designed for search queries, not the user's own message. Measurable via audit log per ruleId — watch the block rate in Part 3 before trusting gate metrics. | entityContainment.js:155-210 (Signals 1-3); calendarGate.test.ts:50-57 |
| 4 | **CONFIRMED-OK** (1 gap) | **Audit log: no user content can land, rotation caps, writes serialized.** Persisted fields only: `turnId`(optional)/`toolName`/`ruleId`/`action`/`outcome` (gateAuditLog.ts:13-19; sanitize :32-41). ruleId ∈ fixed code literals; toolName ∈ {web_search, calendar_agenda} — unregistered tools never reach `appendGateAudit` (runToolGate.ts:98-99 + toolGateRegistry.ts:27-29); action/outcome enum-validated; `parseList` re-sanitizes legacy rows, dropping any unknown fields (:54-69). Query/fact/range text is structurally excluded: the call passes decision-derived fields only (runToolGate.ts:114-121). Snapshot/trace stay in memory, never persisted (grep: no trace serialization). Rotation: `slice(-500)` at cap 500 (:10,:76), test asserts newest-kept (gateAuditLog.test.ts:23-30). Concurrency: module-level `writeChain` serializes RMW (:81-88); `clearGateAudit` chained (:99-103); failures swallowed best-effort, never throw into the tool (:86-88). **Gap:** production never passes `turnId` (AppShell.tsx:1973-1979) → on-device entries are not turn-linkable (telemetry lines carry turnId); tests use it (runToolGate.test.ts:38). Diagnosability gap, not privacy — defer (or pass turnId: one arg). | gateAuditLog.ts:10-19,32-41,54-88; AppShell.tsx:1973-1979 |
| 5 | **CONFIRMED-OK** | **Registry exemptions are BY CONSTRUCTION.** Registry contains exactly `web_search` + `calendar_agenda` (toolGateRegistry.ts:9-16). miniapp/document_chat/web_fetch are absent → `resolveToolGateTable` returns undefined → pass-through with no audit — no ad-hoc exemption if-checks exist anywhere (the only conditional is web_search's always-on resolution :27, which is the flag-off contract, not an exemption). New tools default UNGATED (fail-open): a future tool can neither accidentally bypass (gating requires registration) nor accidentally gate. Tests cover expand-on/off + document_chat/web_fetch/miniapp (toolGateRegistry.test.ts:19-23; runToolGate.test.ts:52-61). | toolGateRegistry.ts:9-29 |
| 6 | **CONFIRMED-OK** | **Warn: wired but unreachable in production — and correct anyway.** No production table has `kind:"warn"` (toolGate.ts, calendarGate.ts: block only) → warn never fires today; per spec ("ship block + warn"; inject deferred). `applyWarnToResult` is correct: spread-preserves result shape, prepends `${note}\n\n${text}` (runToolGate.ts:62-75); downstream parsers untouched (result-message wrapping is text-only; per #1, no stream). Tests are real, not tautological: evaluate.test.ts:16-40 (warn doesn't block; block wins over warn), runToolGate.test.ts:63-70 (prepend + shape preservation incl. `kind`). | runToolGate.ts:62-75,131-135; evaluate.ts:126-137 |
| 7 | **CONFIRMED-OK** | **No regression of sealed S1/S3.** S3 files byte-unchanged since seal (mtimes ≤ 17:33:32 vs S3 fix pass 17:31-17:33): dnaBounding.ts 17:32:31 (+test 17:33:32), LlamaService 17:32:23, sessionPersistence 17:31:24, MemoryStore 17:20:17, memoryTelemetry 17:20:03, memoryFactsTail 17:23:44. S3-mandated lines intact: both restore sites use `memoryFactTextsForEnvHash` (AppShell.tsx:3256-3262, :3777-3783); **no `.slice(-10)` remains in src/app|src/engine** (grep exit 1); 40-fact worst-case + greedy-boundary tests present (dnaBounding.test.ts:164-198). S1-sealed files untouched: toolGate.ts Aug 14, SettingsScreen 17:04, compactor/ttftFlags/digestTelemetry/turnTelemetry 16:45. S4 touched only: AppShell 17:53, webSearchTool 17:52, en/it, rules/* 17:48-17:52. Caveat: tree uncommitted — scope by mtime, tight and consistent. Cosmetic: LlamaService.ts:3120-3126 comment ("webSearchTool's only use of this argument") is now false — gate moved to AppShell. | mtimes; AppShell.tsx:3256,3777 |
| 8 | **CONFIRMED-OK** | **i18n en+it complete; tsc+jest re-run by auditor.** 3 new keys both locales: `calendarRangeInvalid` (en:683/it:681), `toolPrivacyBlocked` (en:685/it:683), `results.toolWarnedPrivacy` (en:837/it:811). `it: typeof en` → symmetry compiler-enforced; `Locale = "en"\|"it"` (types.ts:3). **`npx tsc --noEmit` → exit 0; `npx jest src/rules/ src/memory/` → 7 suites / 27 tests, exit 0.** | — |
| 9 | **DEFECT** (pre-existing, cemented by S4) | **`kalsa.bench.toolgate=0` is dead — the CI A/B "echo gate off" arm cannot be measured.** Contract: knob disables the echo-of-context veto (benchConfig.ts:464-472). Mechanism: LlamaService blanks the 4th `lastUserMessage` arg (LlamaService.ts:2553,3126). But executeTool reads `lastUserRawRef.current` for the gate (AppShell.tsx:1936-1939) — the 4th arg is now entirely unused (searchExec's `_lastUserMessage` too). Knob was already inert at HEAD (rawUserText precedence existed then, HEAD AppShell:1928-1932) — NOT an S4 regression — but S4's chokepoint move removed the last structurally functional hook (the engine-side argument), so the fix belongs here. Silent no-op falsifies any CI A/B that thinks it disables the gate. | LlamaService.ts:3120-3126; AppShell.tsx:1936-1939; benchConfig.ts:460-472 |

## Must fix before campaign

1. **F-9 (3 lines):** in `executeTool`, honor the bench knob — `lastUserMessage: toolGateEnabled ? rawUserText : ""` for the gate (import `getToolGateEnabled`), or delete the knob + LlamaService blanking with a comment. Silent no-op is the worst state; Part-3 CI A/B integrity depends on it.
2. **F-3a (1 line, owner decision):** align the range gate with the tool's own contract — either (a) mark `fromISO`/`toISO` `required: true` in the schema (gate then matches what the model must supply), or (b) drop `calendar-empty-range`/`calendar-malformed-range` and let `resolveAgendaRange`'s tolerant semantics rule, keeping only `calendar-private-data`. As shipped, flag-ON blocks every schema-permitted "default: today" call the tool is documented to serve — §1.1 blunt-gate failure mode, guaranteed to hit probe_tool turns.

## Can defer

- **F-3b** calendar containment overblock — spec'd; has a ruleId in the audit log; measure the block rate in Part 3; if high, soften to warn for calendar.
- **F-4 gap** — pass `turnId` through to `runToolGate` (:1973) so audit rows link to telemetry lines; one arg, no privacy impact.
- **F-7 cosmetic** — fix the stale LlamaService.ts:3120 comment; delete nothing else.

## Summary

| Category | Count |
|---|---|
| CONFIRMED-OK | 7 (findings 1,2,4,5,6,7,8 — with 3b risk note) |
| DEFECT | 2 (findings 3a, 9) |
| PLAUSIBLE-RISK | 1 (finding 3b) |

## Final verdict: **FIX-AGAIN** (two small fixes: bench-knob rewiring + range-gate/schema alignment; both before campaign). Everything else verified hostile-clean; S1/S3 seals undisturbed; tsc exit 0, jest 27/27 exit 0.