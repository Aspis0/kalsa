# AUDIT S3 — memory-leg S3 changes (uncommitted, 2026-08-25 session)

Scope: `src/memory/dnaBounding.ts` (+test), `memoryTelemetry.ts`, `MemoryStore.ts`, `src/engine/memoryFactsTail.ts`, touchpoints `AppShell.tsx`, `LlamaService.ts`. Reference: `ciswire/src/index.ts:463-521` (`computeDnaDeferral`) / `:523+` (`boundDnaAppendix`).

Verification runs (item 8): `npx tsc --noEmit` → **exit 0**; `npx jest src/memory/` → **2 suites / 10 tests pass, exit 0**.

## Findings

| # | Verdict | Finding | Evidence |
|---|---|---|---|
| 1a | CONFIRMED-OK | Newest-date-first ranking identical to CisWire: `date` desc, `index` asc tie-break | kalsa `dnaBounding.ts:112-115` ‖ ciswire `index.ts:483-487` — same comparator, byte-for-byte semantics |
| 1b | CONFIRMED-OK | Freeze determinism: same fact set → identical output. All-fit path returns ORIGINAL order (not re-ranked) — matches CisWire returning raw appendix unchanged; rank path is date/index deterministic, no `Date.now()`/randomness anywhere in `boundMemoryFacts` | kalsa `dnaBounding.ts:102-110,133-136`; ciswire `index.ts:468-469,504-506` |
| 1c | CONFIRMED-OK | Whole-fact deferral: greedy `used + t <= effective` fits or drops the entire note; never slices mid-fact | kalsa `dnaBounding.ts:121-126` ‖ ciswire `index.ts:491-496` |
| 1d | CONFIRMED-OK (accepted deviation) | Marker shape matches, wording adapted: kalsa `\n- […] N older notes deferred — see Memory\n` (`:56-58`) vs ciswire `\n- […] N older DNA notes deferred — see memory.md\n` (`index.ts:489`). Reserve semantics identical: worst-case `notes.length` upfront | kalsa `:117` ‖ ciswire `:488` |
| 1e | DEFECT (doc) | Comment arithmetic wrong: "40 × 200 chars ≈ 1200" — 40×200/4 = **2000**. Intent is 40 × 120 (injection line cap) = 1200. Code is consistent (1200 budget, 120-char cap); only the comment lies. Also note: CisWire default sub-budget is **1800** (`DNA_MAX_TOKENS`, `index.ts:135-137`); 1200 is a Kalsa-specific shrink — defensible (store cap 40), but a deliberate divergence, cite it | `dnaBounding.ts:13`, `:48-53`; `MemoryStore.ts:41` (store allows 200-char facts, truncated to 120 at injection) |
| 1f | PLAUSIBLE-RISK | Estimator: kalsa `ceil(len/4)` (`:19`) vs ciswire CJK-aware `ceil(cjk/1.5 + other/4)` (`distiller.ts:77-94`). On CJK-heavy text kalsa underestimates ~46% (measured: 102-char sample → 26 vs 38). IT/EN facts ≈ ASCII → equivalent. Accepted in-code (`:17` "not CisWire's CJK-aware estimator") but real tokens can exceed the 1200 budget for CJK facts | `dnaBounding.ts:15-19` |
| 1g | PLAUSIBLE-RISK | Missing CisWire's final `hardCapDna` clamp on the assembled output (`index.ts:507-511`). Invariant `output ≤ budget` holds whenever `budget ≥ markerTokens` (production: 1200 ≫ 11). Pathological hole: `budget < marker` → all notes "deferred", output = marker alone, **output exceeds budget** (verified: budget 8, marker ≈ 11t). Reachable only via the test parameter, not production — but the freeze test (`dnaBounding.test.ts:91-99`) exercises exactly this region and asserts nothing about it | `dnaBounding.ts:133-146` |
| 2 | CONFIRMED-OK | DUAL-CALL CONSISTENCY: identical inputs, both sites. Trace: `AppShell.tsx:4861` captures `promptFacts` (snapshot closure) → `:4865` `boundMemoryFacts(promptFacts)` (telemetry) → `:4943` `memoryFacts: promptFacts` → single caller of `streamAssistantTurn` (`AppShell.tsx:4878`) → `LlamaService.ts:2646` `buildMemoryFactsBlock(locale, options.memoryFacts)` → `memoryFactsTail.ts:30` bounds the SAME array. `boundMemoryFacts` is pure; fact objects never mutated (state replaced wholesale); store writes (`applyExtractResults`) and epoch bumps (remove/clear/disable) hit the STORE, not the passed snapshot; extract runs after the turn. Telemetry cannot lie — both sites necessarily produce identical bytes. Residual (pre-existing pattern): toggle memory OFF mid-turn → turn-end line says `memoryEnabled:0` with `factsInjected>0` | `AppShell.tsx:4861-4875,4878,4943`; `LlamaService.ts:2645-2648`; `memoryFactsTail.ts:26-35`; `MemoryStore.ts:397-409` |
| 3 | DEFECT (latent) | 10-fact cap removed at sync (`AppShell.tsx:2414-2418`, old `slice(-10)` gone) but **two env-hash restore sites still `slice(-10)`**: send hash now covers ALL fact texts (`LlamaService.ts:2598-2600`), restore hash only last 10 (`AppShell.tsx:3261,3782`). With `MEMORY_FACTS_ON_USER_TAIL=false` and store >10 facts, `computePromptEnvHash` can NEVER match → every session restore rejected → KV warm-start lost forever (1.8s → 120.8s cold path). Dormant under default (flag ON → both sides `[]`). **Budget math**: "40×200×¼ = 2000 > 1200" premise fails at the injection layer — facts capped at 120 chars; worst case 40×(120+16c overhead)=5440c = 1360t → defers 6, output 1167 ≤ 1200 (computed). Deferral engages only when avg fact > ~104 chars. Steady state at full store with realistic 60–100c facts: **all 40 injected, 760–1160t, no deferral** — i.e. up to 4× the legacy worst case (10×120c ≈ 300t) of prompt cost, but bounded ≤1200 and marker-visible. Sane, but the block never self-limits below ~1200t; watch prefill on-device. No other consumer assumes ≤10: `longChatEstimate` fact-free; Settings lists all facts; echo guard (`AppShell.tsx:1836`) now uses bounded `keptTexts` (more accurate than old raw slice) | computed; `MemoryStore.ts:40` (cap 40) |
| 4 | CONFIRMED-OK (cosmetic caveat) | `toISOString().slice(0,10)` is locale-independent UTC (`dnaBounding.ts:43-45`). Italian user (UTC+1/+2): a fact stored 00:00–02:00 local (CEST) gets labeled with the previous UTC day. Label only — never persisted, never compared to local calendar, ordering internally consistent. No locale bug (no `toLocaleDateString`). `Number.isFinite` guard → `1970-01-01` fallback unreachable from store (JSON cannot carry NaN) | |
| 5 | CONFIRMED-OK | `dna*` follow -1 NOT_APPLICABLE: KALSA_MEMORY_EXTRACT forces -1 on both the settled path (`AppShell.tsx:4257-4261`) and early-abort path (`:4286-4290`); KALSA_MEMORY turn-end shows real counts when memory ON, stays -1 when OFF (`trackMemoryDnaBound` never called → init/reset -1, `MemoryStore.ts:77-80,167-173`). No user content in any new field: counts only (`formatMemoryLine`), `keptTexts` never logged (grep clean), `ciswireFlags` numeric, omitted when 0 | `memoryTelemetry.ts:25-39,59-77` |
| 6 | CONFIRMED-OK | listFacts order stable: cache is always the output of `dedupeAndCap` — sort by `createdAt` asc, `id.localeCompare` tie-break (`MemoryStore.ts:373-385`); `factsEqual` short-circuit prevents re-sorts. Same fact set → same array order every turn → freeze holds. ids are ASCII base36 → tie-break deterministic per engine | `MemoryStore.ts:397-409` |
| 7 | PLAUSIBLE-RISK | Tests meaningful (all-fit order, duplicate-date ranking, freeze, marker count, whole-note drop) but gaps: (a) **no test asserts `estimate(bounded) ≤ budget` on the deferral path** — the budget invariant; (b) "exactly-at-budget" hits the all-fit equality branch, not the greedy boundary (`used + t == effective`); (c) no budget < marker-cost case exposing the 1g hole; (d) no full-store worst case (40×120c seed); (e) no empty-after-sanitize case; (f) no dual-call consistency test (integration property, untestable at unit level — covered by trace in #2). "one-past" derives its budget from the estimator under test (self-consistent, not independent) | `dnaBounding.test.ts:48-146` |
| 8 | CONFIRMED-OK | `tsc --noEmit` exit 0; `jest src/memory` 2 suites/10 tests exit 0 | — |

## Must fix before S4

1. **Env-hash asymmetry** — `AppShell.tsx:3261` and `:3782`: drop `.slice(-10)` so restore projection matches the send site (`LlamaService.ts:2598-2600`, all texts). One-line each; otherwise flag-off + >10 facts permanently defeats session restore.
2. **`dnaBounding.ts:13`** — comment says "40 × 200 chars ≈ 1200"; the arithmetic is false (would be 2000). Say "40 × 120 chars". (While editing, add the missing invariant assertion from item 7a — two lines, closes 1g's visibility hole.)

## Can defer

- 1f CJK estimator divergence — documented; IT/EN facts; revisit only if CJK facts on device.
- 1g hardCap clamp for `budget < marker` — unreachable at budget 1200; covered by the 7a assertion.
- 7 test gaps (b)-(e) — nice-to-have, not gating.
- 4 UTC date-label boundary — cosmetic; one doc sentence in dnaBounding header is enough.
- 3 steady-state behavior (≤1200t, defers only >~104c avg) — that's the A/B hypothesis; measure prefill in Part 3, don't "fix" blind.
# RE-AUDIT (post-fix pass, same session) — verdict: SEALED

Reference: fix files mtimes bracket vs AUDIT write (17:27:53): sessionPersistence.ts 17:31:24, LlamaService.ts 17:32:23, AppShell.tsx 17:32:28, dnaBounding.ts 17:32:31, dnaBounding.test.ts 17:33:32. Verified 2026-08-25.

| # | Verdict | Evidence |
|---|---|---|
| 1 | RESOLVED | Both restore sites now `computePromptEnvHash(locale, memoryFactTextsForEnvHash(facts), …)` — `AppShell.tsx:3260-3262`, `:3781-3783`; no `.slice(-10)` remains in `src/app` (grep clean). Helper `sessionPersistence.ts:313-320` = `(facts ?? []).map(f => f.text)` — order-preserving, `.text` extraction, null-safe. Send path hashes the identical helper on the same array family: `LlamaService.ts:2600-2601` over `options.memoryFacts` = `promptFacts` = `memoryFactsRef.current` (`AppShell.tsx:4861,4943`), itself `MemoryStore.listFacts()` output (`refreshMemoryFacts` `AppShell.tsx:2415-2418`) — same canonical `dedupeAndCap` order both sides (createdAt asc / id tie-break, `MemoryStore.ts:373-384`). Subtle trap: flag ON → send hashes `[]` (`LlamaService.ts:2599`); both restore sites skip the whole listFacts branch and hash `[]` (`AppShell.tsx:3256-3259`, `:3777-3780`). Identical conditional. |
| 2 | RESOLVED | `dnaBounding.ts:12-16` comment now: "40 × (120 + 16 label) = 5440 chars ≈ 1360 est tokens, so 1200 defers overflow". Arithmetic true: 40×136=5440, ÷4=1360 > 1200. Matches code constants: cap `PROMPT_FACT_CHARS=120` (`:25`), budget `MEMORY_SUB_BUDGET_TOKENS=1200` (`:19`). 16 = "\n- [YYYY-MM-DD] " (label is 15c; 16 is ≥1c/fact conservative, never a lie). Parenthetical 40×200/4=2000 correctly explains why the store-cap 200 path is not the injection cost. |
| 3 | RESOLVED | `expectWithinBudget` (`dnaBounding.test.ts:17-21`) recomputes `estimateMemoryTokens(r.bounded)` on the real output (incl. marker) and asserts `≤ budget` — not vacuous. Runs on every success path (:39,:54,:69,:106,:117,:143,:157) plus the two new required tests: **40-fact/120-char worst case** `:164-174` — 40×135+39 = 5439c ≈ 1360t > 1200 so the deferral path genuinely engages; asserts `deferredCount > 0`, `injected+deferred = 40`, `≤ 1200`. **Greedy exact-boundary** `:176-198` — budget = blockCost + exact markerReserve; asserts injected=1/deferred=1 and `keptTexts=[newest]`, which fails if the loop used strict `<` — the `used+t == effective` equality is exercised for real. 1g hole (budget < marker) still documented-not-asserted at `:91-99` — per audit, deferred. |
| 4 | CONFIRMED | Scope: only the 5 claimed files changed post-audit (mtimes ≥17:31 vs audit 17:27:53); every other working-tree modification (compactor.ts/ui/telemetry/i18n/SettingsScreen/MemoryStore/memoryFactsTail/ciswireFlagsTelemetry.test etc.) is ≤17:23, i.e. pre-audit S3-session work. Caveat: no commit boundary (tree uncommitted) — scope by mtime, tight and consistent. **Stored-hash invalidation, honest verdict: LOW.** Flag ON (default, `ttftFlags.ts:23`) hashes `[]` before and after → zero impact for default users. Flag OFF: pre-update send hashed the sync-capped last-10 texts; store ≤10 facts → same string set, same order → old stored hash still matches → no miss. Store >10 facts → old stored hash (10 texts) ≠ new recompute (all) → **one** restore miss per pre-existing session (one cold prefill, graceful: `LlamaService.ts:2127` `meta_mismatch:promptEnvHash` → stem not loaded), self-healing on that session's next save (new-shape hash persisted, `LlamaService.ts:2314`). One-time, no data loss, flag-off only. Residual (pre-existing, not introduced by fix): restore sites hardcode `hasTools: true` vs send `Boolean(tools?.length && executeTool)` (`LlamaService.ts:2559`) — no practical divergence (AppShell always wires `executeTool`, `:1932`); transient extract-between-restore-and-send race → one miss, self-healing, same as audit. |
| 5 | CONFIRMED | `npx tsc --noEmit` → **exit 0**. `npx jest src/memory/` → **2 suites / 12 tests pass (was 10 — the two new tests are the 40×120 worst case + greedy boundary), exit 0**. |

## Final verdict: **SEALED** — all 3 must-fixes resolved with real evidence; S4 may proceed. Flag-off + >10-fact users take one self-healing cold prefill post-update (acceptable; note it in release notes if flag-off is user-facing).
